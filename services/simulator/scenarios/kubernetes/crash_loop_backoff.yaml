scenario:
  name: crash_loop_backoff
  category: kubernetes

trigger:
  alert:
    name: KubernetesContainerCrashLooping

expected_evidence:
  - pod_status
  - container_restart_count
  - kubernetes_event
  - container_logs

expected_rca:
  causes:
    - application_start_failure
    - configuration_error

verification:
  conditions:
    - pod_ready
