# Web Gateway API Integration

Incident Center will consume Gateway APIs through this layer.

Expected APIs:

- GET /api/incidents
- GET /api/incidents/{incident_id}
- GET /api/investigation/{incident_id}
- GET /api/evidence/{incident_id}

The frontend currently keeps mock data until backend routes are exposed.
