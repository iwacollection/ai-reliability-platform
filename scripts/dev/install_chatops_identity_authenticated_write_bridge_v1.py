from __future__ import annotations

import hashlib
import shutil
import subprocess
import traceback

from dataclasses import dataclass
from datetime import datetime
from pathlib import Path


VERSION = (
    "chatops-identity-authenticated-write-bridge-v1"
)

AFTER_NAME = (
    "chatops_identity_authenticated_write_bridge_v1_after.txt"
)

ERROR_NAME = (
    "chatops_identity_authenticated_write_bridge_v1_error.txt"
)

EXPECTED_RAW_HASHES = {'services/agent_runtime/app/runtime/runtime.py': '3673ad0ba9d3425924fcec7edb48f7ae8c45b4ca0ff24bfd0148f8f6d3dc12e2', 'services/agent_runtime/app/conversation/__init__.py': '7636eddef71b6f6a53f770af2758785803e367c1992ec1b74c734ee0d1b6ec90', 'services/agent_runtime/app/conversation/orchestrator.py': '08f69f5f7f55e2b836bb10b3020a158e61f86ac442f3fe92e12ba89665df42e9', 'services/agent_runtime/app/conversation/chatops.py': '3c73a9a86bc34712a77ac3ea3196e44ee355989f0b869b73500e83d791d80966', 'services/agent_runtime/tests/test_conversation_orchestrator.py': 'ded3155653ec841c2664172a359b37aab50430aa60a9c47bd4c5619794f1491a', 'services/agent_runtime/app/security/authentication.py': '41db90332e808cc45616b37b9284e9903a1da20b8f9528595fca045aac8d5022', 'services/agent_runtime/app/security/models.py': 'c246fe2f9cc2410969c97c6af784593ab4a2ce0a33f0911b4861baf0e8196755', 'services/agent_runtime/app/security/policy.py': 'cb8025a63839cde03b1b04da368675328050431dd1fce499499210d3d819e5d5', 'services/agent_runtime/app/security/service.py': 'b11ba60f2c5a496c839ff6179aed871f4cdb9860a1b8d9da80fffdf2d126c5a8', 'services/agent_runtime/app/security/api.py': '8e862100e4993bdca6472667aba18f84117862ea589556f3116ff6ce230d31b7', 'services/agent_runtime/app/approval/service.py': '9ee73e7cda390d2237dcea6fadf5076b17a14ab20b815d4a8cd6a34fc0f65bff', 'services/agent_runtime/app/approval/manager.py': '8e48675c9267f9e9fe0e0bdce71495ed7478cde531b6227fbcc26581b55633dc', 'services/agent_runtime/app/runtime/action_runtime.py': '62201639245e44de21ea21d68be3d91027728d0515218a1f96cbac06b0defcfd', 'services/agent_runtime/app/api/runtime.py': 'db8322e750deb76c709a1da89e3d01124f0e52e6c255bcca9bf5fbbbbca73d88', 'services/agent_runtime/tests/test_api_security_matrix.py': 'c282398cf6e58d5ca20e2c1eb04ac38a37dbe34876c276fa46a291d991d2af6f'}
SOURCES = {'services/agent_runtime/app/conversation/identity.py': 'from __future__ import annotations\n\nimport hashlib\nimport json\nimport os\n\nfrom abc import ABC, abstractmethod\nfrom collections.abc import Iterable\nfrom typing import Annotated, Any\n\nfrom pydantic import (\n    BaseModel,\n    ConfigDict,\n    StringConstraints,\n    model_validator,\n)\n\nfrom services.agent_runtime.app.conversation.chatops import (\n    ChatOpsInboundMessage,\n)\nfrom services.agent_runtime.app.security.authentication import (\n    AuthenticationError,\n)\nfrom services.agent_runtime.app.security.models import (\n    AuthorizationDecision,\n    OperatorIdentity,\n    ProtectedOperation,\n)\nfrom services.agent_runtime.app.security.policy import (\n    AuthorizationDeniedError,\n    SecurityPolicyEngine,\n)\nfrom services.agent_runtime.app.security.service import (\n    AuthenticationProviderContractError,\n    AuthenticationProviderExecutionError,\n    AuthenticationProviderUnavailableError,\n    AuthenticationService,\n    AuthenticationServiceConfigurationError,\n)\n\n\nShortText = Annotated[\n    str,\n    StringConstraints(\n        strip_whitespace=True,\n        min_length=1,\n        max_length=256,\n    ),\n]\n\n\nclass ChatOpsIdentityError(RuntimeError):\n    """Base fail-closed ChatOps identity error."""\n\n\nclass ChatOpsActorVerificationError(\n    ChatOpsIdentityError\n):\n    """A channel actor could not be verified."""\n\n\nclass ChatOpsIdentityBindingError(\n    ChatOpsIdentityError\n):\n    """A verified channel actor has no safe Runtime identity binding."""\n\n\nclass ChatOpsIdentityAuthenticationError(\n    ChatOpsIdentityError\n):\n    """The configured Runtime credential could not authenticate safely."""\n\n\nclass ChatOpsAuthorizationDeniedError(\n    ChatOpsIdentityError\n):\n    """The authenticated Runtime principal is not allowed to write."""\n\n\nclass ChatOpsVerifiedActor(BaseModel):\n    """\n    Channel actor identity emitted only by a trusted verifier.\n\n    No Runtime role or permission is carried here. Those remain owned by the\n    existing AuthenticationService and SecurityPolicyEngine.\n    """\n\n    model_config = ConfigDict(\n        frozen=True,\n        extra="forbid",\n    )\n\n    channel: ShortText\n    tenant_id: ShortText | None = None\n    external_actor_id: ShortText\n    verification_method: ShortText\n\n    def actor_fingerprint(\n        self,\n    ) -> str:\n        payload = json.dumps(\n            [\n                self.channel,\n                self.tenant_id or "",\n                self.external_actor_id,\n            ],\n            ensure_ascii=False,\n            separators=(\n                ",",\n                ":",\n            ),\n        )\n\n        return hashlib.sha256(\n            payload.encode(\n                "utf-8"\n            )\n        ).hexdigest()\n\n\nclass BaseChatOpsActorVerifier(\n    ABC\n):\n    """\n    Trusted channel-transport actor verifier contract.\n\n    Concrete Feishu/DingTalk/Slack adapters must verify their transport before\n    returning ChatOpsVerifiedActor. The core has no permissive default.\n    """\n\n    @abstractmethod\n    async def verify(\n        self,\n        message: ChatOpsInboundMessage,\n    ) -> ChatOpsVerifiedActor:\n        raise NotImplementedError\n\n\nclass ChatOpsIdentityBinding(BaseModel):\n    """\n    Operator-configured bridge from one verified channel actor to an existing\n    Runtime authentication credential reference.\n\n    credential_env contains only an environment variable name. It never stores\n    the credential value and does not assign roles or permissions.\n    """\n\n    model_config = ConfigDict(\n        frozen=True,\n        extra="forbid",\n    )\n\n    channel: ShortText\n    tenant_id: ShortText | None = None\n    external_actor_id: ShortText\n    expected_principal_id: ShortText\n    credential_env: ShortText\n    provider_name: ShortText | None = None\n\n    @model_validator(\n        mode="after"\n    )\n    def validate_environment_reference(\n        self,\n    ) -> "ChatOpsIdentityBinding":\n        if any(\n            character.isspace()\n            for character\n            in self.credential_env\n        ):\n            raise ValueError(\n                "ChatOps credential environment reference is invalid"\n            )\n\n        return self\n\n    def key(\n        self,\n    ) -> tuple[\n        str,\n        str,\n        str,\n    ]:\n        return (\n            self.channel,\n            self.tenant_id\n            or "",\n            self.external_actor_id,\n        )\n\n\nclass ChatOpsIdentityBindingRegistry:\n    """Immutable exact-match registry for verified actor identity bindings."""\n\n    def __init__(\n        self,\n        bindings: Iterable[\n            ChatOpsIdentityBinding\n        ],\n    ) -> None:\n        values = tuple(\n            ChatOpsIdentityBinding\n            .model_validate(\n                item\n            )\n            for item in bindings\n        )\n\n        if not values:\n            raise ValueError(\n                "ChatOps identity binding registry cannot be empty"\n            )\n\n        mapping: dict[\n            tuple[\n                str,\n                str,\n                str,\n            ],\n            ChatOpsIdentityBinding,\n        ] = {}\n\n        for item in values:\n            key = item.key()\n\n            if key in mapping:\n                raise ValueError(\n                    "Duplicate ChatOps identity binding"\n                )\n\n            mapping[\n                key\n            ] = item\n\n        self._bindings = values\n        self._mapping = mapping\n\n    @property\n    def binding_count(\n        self,\n    ) -> int:\n        return len(\n            self._bindings\n        )\n\n    def get(\n        self,\n        actor: ChatOpsVerifiedActor,\n    ) -> ChatOpsIdentityBinding:\n        if not isinstance(\n            actor,\n            ChatOpsVerifiedActor,\n        ):\n            raise ChatOpsIdentityBindingError(\n                "ChatOps verified actor is invalid"\n            )\n\n        key = (\n            actor.channel,\n            actor.tenant_id\n            or "",\n            actor.external_actor_id,\n        )\n\n        binding = self._mapping.get(\n            key\n        )\n\n        if binding is None:\n            raise ChatOpsIdentityBindingError(\n                "Verified ChatOps actor is not bound to a Runtime identity"\n            )\n\n        return binding\n\n\nclass ChatOpsIdentityAuthenticator:\n    """\n    Authenticate one verified actor through the existing AuthenticationService.\n\n    The configured environment credential is read only for the duration of the\n    authentication call and is never retained or serialized by this object.\n    """\n\n    def __init__(\n        self,\n        *,\n        authentication: AuthenticationService,\n        bindings: ChatOpsIdentityBindingRegistry,\n    ) -> None:\n        if not isinstance(\n            authentication,\n            AuthenticationService,\n        ):\n            raise TypeError(\n                "ChatOps identity authenticator requires AuthenticationService"\n            )\n\n        if not isinstance(\n            bindings,\n            ChatOpsIdentityBindingRegistry,\n        ):\n            raise TypeError(\n                "ChatOps identity authenticator requires binding registry"\n            )\n\n        self.authentication = authentication\n        self.bindings = bindings\n\n    def authenticate(\n        self,\n        actor: ChatOpsVerifiedActor,\n    ) -> OperatorIdentity:\n        binding = self.bindings.get(\n            actor\n        )\n\n        credential = os.environ.get(\n            binding.credential_env\n        )\n\n        if (\n            not isinstance(\n                credential,\n                str,\n            )\n            or not credential\n        ):\n            raise ChatOpsIdentityAuthenticationError(\n                "ChatOps Runtime authentication is unavailable"\n            )\n\n        try:\n            identity = (\n                self.authentication\n                .authenticate(\n                    credential,\n                    provider_name=(\n                        binding.provider_name\n                    ),\n                )\n            )\n\n        except (\n            AuthenticationError,\n            AuthenticationProviderUnavailableError,\n            AuthenticationProviderExecutionError,\n            AuthenticationProviderContractError,\n            AuthenticationServiceConfigurationError,\n        ):\n            raise ChatOpsIdentityAuthenticationError(\n                "ChatOps Runtime authentication failed"\n            ) from None\n\n        except Exception:\n            raise ChatOpsIdentityAuthenticationError(\n                "ChatOps Runtime authentication is unavailable"\n            ) from None\n\n        if (\n            identity.principal_id\n            != binding.expected_principal_id\n        ):\n            raise ChatOpsIdentityAuthenticationError(\n                "ChatOps Runtime principal binding does not match"\n            )\n\n        return identity\n\n\nclass ChatOpsSecurityContext(BaseModel):\n    """Credential-free authenticated and authorized ChatOps write context."""\n\n    model_config = ConfigDict(\n        frozen=True,\n        extra="forbid",\n    )\n\n    verified_actor: ChatOpsVerifiedActor\n    identity: OperatorIdentity\n    authorization: AuthorizationDecision\n\n    @model_validator(\n        mode="after"\n    )\n    def validate_context(\n        self,\n    ) -> "ChatOpsSecurityContext":\n        if not self.identity.authenticated:\n            raise ValueError(\n                "ChatOps security context requires authenticated identity"\n            )\n\n        if not self.authorization.allowed:\n            raise ValueError(\n                "ChatOps security context requires allowed authorization"\n            )\n\n        if (\n            self.authorization.principal_id\n            != self.identity.principal_id\n        ):\n            raise ValueError(\n                "ChatOps identity and authorization do not match"\n            )\n\n        return self\n\n    @property\n    def principal_id(\n        self,\n    ) -> str:\n        return self.identity.principal_id\n\n    def stable_audit_context(\n        self,\n    ) -> dict[str, Any]:\n        """\n        Stable credential-free audit metadata suitable for idempotent decisions.\n\n        Dynamic authentication timestamps are deliberately excluded because the\n        exact same ChatOps webhook replay must produce the same decision data.\n        """\n\n        return {\n            "principal_id": (\n                self.identity.principal_id\n            ),\n            "authentication_method": (\n                self.identity.authentication_method.value\n            ),\n            "roles": sorted(\n                role.value\n                for role\n                in self.identity.roles\n            ),\n            "protected_operation": (\n                self.authorization.operation.value\n            ),\n            "policy_version": (\n                self.authorization.policy_version\n            ),\n            "chatops_channel": (\n                self.verified_actor.channel\n            ),\n            "chatops_actor_fingerprint": (\n                self.verified_actor\n                .actor_fingerprint()\n            ),\n            "chatops_verification_method": (\n                self.verified_actor\n                .verification_method\n            ),\n        }\n\n\nclass ChatOpsSecurityAdapter:\n    """\n    Verify channel actor, authenticate via Runtime AuthenticationService, then\n    authorize with the existing SecurityPolicyEngine before domain work.\n    """\n\n    def __init__(\n        self,\n        *,\n        verifier: BaseChatOpsActorVerifier,\n        authenticator: ChatOpsIdentityAuthenticator,\n        policy: SecurityPolicyEngine,\n    ) -> None:\n        if not isinstance(\n            verifier,\n            BaseChatOpsActorVerifier,\n        ):\n            raise TypeError(\n                "ChatOps actor verifier is invalid"\n            )\n\n        if not isinstance(\n            authenticator,\n            ChatOpsIdentityAuthenticator,\n        ):\n            raise TypeError(\n                "ChatOps identity authenticator is invalid"\n            )\n\n        if not isinstance(\n            policy,\n            SecurityPolicyEngine,\n        ):\n            raise TypeError(\n                "ChatOps security policy is invalid"\n            )\n\n        self.verifier = verifier\n        self.authenticator = authenticator\n        self.policy = policy\n\n    async def require(\n        self,\n        message: ChatOpsInboundMessage,\n        operation: ProtectedOperation,\n    ) -> ChatOpsSecurityContext:\n        if not isinstance(\n            message,\n            ChatOpsInboundMessage,\n        ):\n            raise ChatOpsActorVerificationError(\n                "ChatOps inbound message is invalid"\n            )\n\n        if message.external_actor_id is None:\n            raise ChatOpsActorVerificationError(\n                "ChatOps write requires a verified external actor"\n            )\n\n        try:\n            actor = await self.verifier.verify(\n                message\n            )\n\n        except ChatOpsIdentityError:\n            raise\n\n        except Exception:\n            raise ChatOpsActorVerificationError(\n                "ChatOps actor verification failed"\n            ) from None\n\n        if not isinstance(\n            actor,\n            ChatOpsVerifiedActor,\n        ):\n            raise ChatOpsActorVerificationError(\n                "ChatOps actor verifier returned an invalid result"\n            )\n\n        if (\n            actor.channel\n            != message.conversation.channel\n            or actor.tenant_id\n            != message.conversation.tenant_id\n            or actor.external_actor_id\n            != message.external_actor_id\n        ):\n            raise ChatOpsActorVerificationError(\n                "Verified ChatOps actor does not match the inbound message"\n            )\n\n        identity = (\n            self.authenticator\n            .authenticate(\n                actor\n            )\n        )\n\n        try:\n            decision = self.policy.require(\n                identity,\n                ProtectedOperation(\n                    operation\n                ),\n            )\n\n        except AuthorizationDeniedError:\n            raise ChatOpsAuthorizationDeniedError(\n                "ChatOps operator is not authorized for this operation"\n            ) from None\n\n        return ChatOpsSecurityContext(\n            verified_actor=actor,\n            identity=identity,\n            authorization=decision,\n        )\n\n\n__all__ = [\n    "BaseChatOpsActorVerifier",\n    "ChatOpsActorVerificationError",\n    "ChatOpsAuthorizationDeniedError",\n    "ChatOpsIdentityAuthenticationError",\n    "ChatOpsIdentityBinding",\n    "ChatOpsIdentityBindingError",\n    "ChatOpsIdentityBindingRegistry",\n    "ChatOpsIdentityAuthenticator",\n    "ChatOpsIdentityError",\n    "ChatOpsSecurityAdapter",\n    "ChatOpsSecurityContext",\n    "ChatOpsVerifiedActor",\n]\n', 'services/agent_runtime/app/conversation/write_bridge.py': 'from __future__ import annotations\n\nimport hashlib\nimport json\n\nfrom enum import Enum\nfrom typing import Any\n\nfrom pydantic import (\n    BaseModel,\n    ConfigDict,\n    Field,\n)\n\nfrom services.agent_runtime.app.action.execution_models import (\n    ActionExecutionRecord,\n)\nfrom services.agent_runtime.app.action.execution_service import (\n    ActionExecutionService,\n)\nfrom services.agent_runtime.app.approval.manager import (\n    ApprovalDecisionConflictError,\n)\nfrom services.agent_runtime.app.approval.models import (\n    ApprovalRequest,\n    ApprovalStatus,\n)\nfrom services.agent_runtime.app.approval.service import (\n    ApprovalService,\n)\nfrom services.agent_runtime.app.conversation.chatops import (\n    ChatOpsInboundMessage,\n)\nfrom services.agent_runtime.app.conversation.identity import (\n    ChatOpsActorVerificationError,\n    ChatOpsAuthorizationDeniedError,\n    ChatOpsIdentityAuthenticationError,\n    ChatOpsIdentityBindingError,\n    ChatOpsSecurityAdapter,\n    ChatOpsSecurityContext,\n)\nfrom services.agent_runtime.app.conversation.models import (\n    ConversationIntent,\n)\nfrom services.agent_runtime.app.conversation.orchestrator import (\n    ConversationOrchestrator,\n)\nfrom services.agent_runtime.app.incident.store import (\n    IncidentStore,\n)\nfrom services.agent_runtime.app.runtime.action_runtime import (\n    ActionRuntime,\n)\nfrom services.agent_runtime.app.security.models import (\n    ProtectedOperation,\n)\nfrom services.agent_runtime.app.verification.coordinator import (\n    VerificationCoordinator,\n)\nfrom services.agent_runtime.app.verification.service import (\n    VerificationService,\n)\n\n\nclass ChatOpsWriteStatus(str, Enum):\n    NO_WRITE_INTENT = "no_write_intent"\n    INCIDENT_REQUIRED = "incident_required"\n    ACTOR_VERIFICATION_FAILED = (\n        "actor_verification_failed"\n    )\n    AUTHENTICATION_FAILED = (\n        "authentication_failed"\n    )\n    AUTHORIZATION_DENIED = (\n        "authorization_denied"\n    )\n    APPROVAL_NOT_FOUND = (\n        "approval_not_found"\n    )\n    APPROVAL_AMBIGUOUS = (\n        "approval_ambiguous"\n    )\n    APPROVAL_REQUIRED = (\n        "approval_required"\n    )\n    APPROVED = "approved"\n    REJECTED = "rejected"\n    EXISTING_EXECUTION = (\n        "existing_execution"\n    )\n    EXECUTION_COMPLETED = (\n        "execution_completed"\n    )\n    EXECUTION_BLOCKED = (\n        "execution_blocked"\n    )\n    VERIFICATION_FOLLOW_UP_REQUIRED = (\n        "verification_follow_up_required"\n    )\n    CONFLICT = "conflict"\n    WRITE_FAILED = "write_failed"\n\n\nclass ChatOpsWriteOutcome(BaseModel):\n    """Sanitized result returned to the future channel renderer."""\n\n    model_config = ConfigDict(\n        frozen=True,\n        extra="forbid",\n    )\n\n    schema_version: str = "v1"\n    success: bool\n    status: ChatOpsWriteStatus\n    operation: str | None = None\n\n    incident_id: str | None = None\n    approval_id: str | None = None\n    execution_id: str | None = None\n    execution_status: str | None = None\n    verification_id: str | None = None\n    verification_status: str | None = None\n\n    operator_id: str | None = None\n    idempotent_replay: bool = False\n\n    failure_code: str | None = Field(\n        default=None,\n        max_length=256,\n    )\n\n    message: str = Field(\n        min_length=1,\n        max_length=1000,\n    )\n\n\nclass ChatOpsAuthenticatedWriteBridge:\n    """\n    Authenticated Human-in-the-loop ChatOps write bridge.\n\n    Security order is fail-closed:\n    1. classify the write intent without domain reads;\n    2. resolve only durable conversation binding;\n    3. verify channel actor;\n    4. authenticate through existing AuthenticationService;\n    5. authorize exact ProtectedOperation through SecurityPolicyEngine;\n    6. only then read/mutate Approval/Incident/Action/Verification state.\n\n    No raw channel actor identifier, credential, token, or platform\n    signature is persisted by this bridge.\n    """\n\n    _INTENT_OPERATION = {\n        ConversationIntent.APPROVE: (\n            "approval.approve",\n            ProtectedOperation.DECIDE_APPROVAL,\n        ),\n        ConversationIntent.REJECT: (\n            "approval.reject",\n            ProtectedOperation.DECIDE_APPROVAL,\n        ),\n        ConversationIntent.REMEDIATE: (\n            "action.resume",\n            ProtectedOperation.RESUME_ACTION,\n        ),\n    }\n\n    def __init__(\n        self,\n        *,\n        orchestrator: ConversationOrchestrator,\n        security: ChatOpsSecurityAdapter,\n        approval_service: ApprovalService,\n        incident_store: IncidentStore,\n        action_runtime: ActionRuntime,\n        action_execution_service: ActionExecutionService,\n        verification_service: VerificationService,\n        verification_coordinator: VerificationCoordinator,\n    ) -> None:\n        dependencies = (\n            (\n                orchestrator,\n                ConversationOrchestrator,\n                "Conversation Orchestrator",\n            ),\n            (\n                security,\n                ChatOpsSecurityAdapter,\n                "ChatOps security adapter",\n            ),\n            (\n                approval_service,\n                ApprovalService,\n                "Approval service",\n            ),\n            (\n                incident_store,\n                IncidentStore,\n                "Incident store",\n            ),\n            (\n                action_runtime,\n                ActionRuntime,\n                "Action Runtime",\n            ),\n            (\n                action_execution_service,\n                ActionExecutionService,\n                "Action Execution service",\n            ),\n            (\n                verification_service,\n                VerificationService,\n                "Verification service",\n            ),\n            (\n                verification_coordinator,\n                VerificationCoordinator,\n                "Verification Coordinator",\n            ),\n        )\n\n        for value, expected, label in dependencies:\n            if not isinstance(\n                value,\n                expected,\n            ):\n                raise TypeError(\n                    f"{label} is invalid"\n                )\n\n        self.orchestrator = orchestrator\n        self.security = security\n        self.approval_service = (\n            approval_service\n        )\n        self.incident_store = (\n            incident_store\n        )\n        self.action_runtime = (\n            action_runtime\n        )\n        self.action_execution_service = (\n            action_execution_service\n        )\n        self.verification_service = (\n            verification_service\n        )\n        self.verification_coordinator = (\n            verification_coordinator\n        )\n\n    async def handle(\n        self,\n        message: ChatOpsInboundMessage,\n    ) -> ChatOpsWriteOutcome:\n        if not isinstance(\n            message,\n            ChatOpsInboundMessage,\n        ):\n            raise TypeError(\n                "ChatOps write requires ChatOpsInboundMessage"\n            )\n\n        intent = (\n            self.orchestrator.classifier\n            .classify(\n                message.text\n            )\n        )\n\n        operation_contract = (\n            self._INTENT_OPERATION.get(\n                intent\n            )\n        )\n\n        if operation_contract is None:\n            return ChatOpsWriteOutcome(\n                success=False,\n                status=(\n                    ChatOpsWriteStatus\n                    .NO_WRITE_INTENT\n                ),\n                message=(\n                    "ChatOps message does not request an authenticated write"\n                ),\n            )\n\n        (\n            operation_name,\n            protected_operation,\n        ) = operation_contract\n\n        binding_key = (\n            message.conversation\n            .binding_key()\n        )\n\n        session = await (\n            self.orchestrator.sessions\n            .get(\n                binding_key\n            )\n        )\n\n        incident_id = (\n            message.incident_id\n            or (\n                session.incident_id\n                if session is not None\n                else None\n            )\n        )\n\n        if incident_id is None:\n            return ChatOpsWriteOutcome(\n                success=False,\n                status=(\n                    ChatOpsWriteStatus\n                    .INCIDENT_REQUIRED\n                ),\n                operation=operation_name,\n                message=(\n                    "ChatOps write requires an Incident binding"\n                ),\n            )\n\n        security = await self._require_security(\n            message,\n            protected_operation,\n            operation_name=(\n                operation_name\n            ),\n            incident_id=incident_id,\n        )\n\n        if isinstance(\n            security,\n            ChatOpsWriteOutcome,\n        ):\n            return security\n\n        # Bind only after the actor is authenticated and authorized.\n        await self.orchestrator.sessions.update(\n            conversation_id=binding_key,\n            incident_id=incident_id,\n            intent=intent,\n        )\n\n        if intent in {\n            ConversationIntent.APPROVE,\n            ConversationIntent.REJECT,\n        }:\n            return await self._decide_approval(\n                message=message,\n                security=security,\n                incident_id=incident_id,\n                approve=(\n                    intent\n                    == ConversationIntent.APPROVE\n                ),\n            )\n\n        return await self._resume_action(\n            message=message,\n            security=security,\n            incident_id=incident_id,\n        )\n\n    async def _require_security(\n        self,\n        message: ChatOpsInboundMessage,\n        protected_operation: ProtectedOperation,\n        *,\n        operation_name: str,\n        incident_id: str,\n    ) -> (\n        ChatOpsSecurityContext\n        | ChatOpsWriteOutcome\n    ):\n        try:\n            return await self.security.require(\n                message,\n                protected_operation,\n            )\n\n        except ChatOpsActorVerificationError:\n            return ChatOpsWriteOutcome(\n                success=False,\n                status=(\n                    ChatOpsWriteStatus\n                    .ACTOR_VERIFICATION_FAILED\n                ),\n                operation=operation_name,\n                incident_id=None,\n                message=(\n                    "ChatOps actor could not be verified"\n                ),\n            )\n\n        except (\n            ChatOpsIdentityBindingError,\n            ChatOpsIdentityAuthenticationError,\n        ):\n            return ChatOpsWriteOutcome(\n                success=False,\n                status=(\n                    ChatOpsWriteStatus\n                    .AUTHENTICATION_FAILED\n                ),\n                operation=operation_name,\n                incident_id=None,\n                message=(\n                    "ChatOps Runtime authentication failed"\n                ),\n            )\n\n        except ChatOpsAuthorizationDeniedError:\n            return ChatOpsWriteOutcome(\n                success=False,\n                status=(\n                    ChatOpsWriteStatus\n                    .AUTHORIZATION_DENIED\n                ),\n                operation=operation_name,\n                incident_id=None,\n                message=(\n                    "ChatOps operator is not authorized for this write"\n                ),\n            )\n\n    async def _decide_approval(\n        self,\n        *,\n        message: ChatOpsInboundMessage,\n        security: ChatOpsSecurityContext,\n        incident_id: str,\n        approve: bool,\n    ) -> ChatOpsWriteOutcome:\n        approvals = await (\n            self.approval_service\n            .list_by_incident(\n                incident_id\n            )\n        )\n\n        desired_status = (\n            ApprovalStatus.APPROVED\n            if approve\n            else ApprovalStatus.REJECTED\n        )\n\n        operation_name = (\n            "approval.approve"\n            if approve\n            else "approval.reject"\n        )\n\n        pending = [\n            item\n            for item in approvals\n            if (\n                item.status\n                == ApprovalStatus.PENDING\n            )\n        ]\n\n        if len(\n            pending\n        ) > 1:\n            return self._approval_ambiguous(\n                security=security,\n                incident_id=incident_id,\n                operation_name=(\n                    operation_name\n                ),\n            )\n\n        if len(\n            pending\n        ) == 1:\n            target = pending[\n                0\n            ]\n            replay = False\n\n        else:\n            target = self._exact_decision_replay(\n                approvals=approvals,\n                message=message,\n                security=security,\n                desired_status=(\n                    desired_status\n                ),\n                operation_name=(\n                    operation_name\n                ),\n            )\n\n            if target is None:\n                return ChatOpsWriteOutcome(\n                    success=False,\n                    status=(\n                        ChatOpsWriteStatus\n                        .APPROVAL_NOT_FOUND\n                    ),\n                    operation=operation_name,\n                    incident_id=incident_id,\n                    operator_id=(\n                        security.principal_id\n                    ),\n                    message=(\n                        "No unique pending Approval is available for this Incident"\n                    ),\n                )\n\n            replay = True\n\n        idempotency_key = (\n            self._idempotency_key(\n                message=message,\n                operation_name=(\n                    operation_name\n                ),\n                target_id=target.id,\n            )\n        )\n\n        if replay:\n            return ChatOpsWriteOutcome(\n                success=True,\n                status=(\n                    ChatOpsWriteStatus.APPROVED\n                    if approve\n                    else ChatOpsWriteStatus.REJECTED\n                ),\n                operation=operation_name,\n                incident_id=incident_id,\n                approval_id=target.id,\n                operator_id=(\n                    security.principal_id\n                ),\n                idempotent_replay=True,\n                message=(\n                    "Exact ChatOps approval decision replayed from durable state"\n                ),\n            )\n\n        metadata = self._audit_metadata(\n            message=message,\n            security=security,\n        )\n\n        reason = self._decision_reason(\n            message.text\n        )\n\n        try:\n            if approve:\n                updated = await (\n                    self.approval_service\n                    .approve(\n                        target.id,\n                        operator_id=(\n                            security.principal_id\n                        ),\n                        idempotency_key=(\n                            idempotency_key\n                        ),\n                        reason=reason,\n                        metadata=metadata,\n                    )\n                )\n\n            else:\n                updated = await (\n                    self.approval_service\n                    .reject(\n                        target.id,\n                        operator_id=(\n                            security.principal_id\n                        ),\n                        idempotency_key=(\n                            idempotency_key\n                        ),\n                        reason=reason,\n                        metadata=metadata,\n                    )\n                )\n\n        except ApprovalDecisionConflictError:\n            return ChatOpsWriteOutcome(\n                success=False,\n                status=(\n                    ChatOpsWriteStatus.CONFLICT\n                ),\n                operation=operation_name,\n                incident_id=incident_id,\n                approval_id=target.id,\n                operator_id=(\n                    security.principal_id\n                ),\n                failure_code=(\n                    "ApprovalDecisionConflictError"\n                ),\n                message=(\n                    "Approval already has a conflicting durable decision"\n                ),\n            )\n\n        except Exception as exc:\n            return ChatOpsWriteOutcome(\n                success=False,\n                status=(\n                    ChatOpsWriteStatus.WRITE_FAILED\n                ),\n                operation=operation_name,\n                incident_id=incident_id,\n                approval_id=target.id,\n                operator_id=(\n                    security.principal_id\n                ),\n                failure_code=(\n                    type(\n                        exc\n                    ).__name__[\n                        :256\n                    ]\n                ),\n                message=(\n                    "Approval decision could not be completed safely"\n                ),\n            )\n\n        return ChatOpsWriteOutcome(\n            success=True,\n            status=(\n                ChatOpsWriteStatus.APPROVED\n                if updated.status\n                == ApprovalStatus.APPROVED\n                else ChatOpsWriteStatus.REJECTED\n            ),\n            operation=operation_name,\n            incident_id=incident_id,\n            approval_id=updated.id,\n            operator_id=(\n                security.principal_id\n            ),\n            idempotent_replay=False,\n            message=(\n                "Approval decision persisted through the authenticated ChatOps bridge"\n            ),\n        )\n\n    async def _resume_action(\n        self,\n        *,\n        message: ChatOpsInboundMessage,\n        security: ChatOpsSecurityContext,\n        incident_id: str,\n    ) -> ChatOpsWriteOutcome:\n        operation_name = (\n            "action.resume"\n        )\n\n        approvals = await (\n            self.approval_service\n            .list_by_incident(\n                incident_id\n            )\n        )\n\n        approved = [\n            item\n            for item in approvals\n            if (\n                item.status\n                == ApprovalStatus.APPROVED\n                and item.action.approved\n                is True\n            )\n        ]\n\n        if len(\n            approved\n        ) > 1:\n            return self._approval_ambiguous(\n                security=security,\n                incident_id=incident_id,\n                operation_name=operation_name,\n            )\n\n        if not approved:\n            pending = any(\n                item.status\n                == ApprovalStatus.PENDING\n                for item in approvals\n            )\n\n            return ChatOpsWriteOutcome(\n                success=False,\n                status=(\n                    ChatOpsWriteStatus\n                    .APPROVAL_REQUIRED\n                    if pending\n                    else ChatOpsWriteStatus\n                    .APPROVAL_NOT_FOUND\n                ),\n                operation=operation_name,\n                incident_id=incident_id,\n                operator_id=(\n                    security.principal_id\n                ),\n                message=(\n                    "Remediation requires one approved Approval before execution"\n                ),\n            )\n\n        approval = approved[\n            0\n        ]\n\n        idempotency_key = (\n            self._idempotency_key(\n                message=message,\n                operation_name=(\n                    operation_name\n                ),\n                target_id=approval.id,\n            )\n        )\n\n        existing_execution = await (\n            self.action_execution_service\n            .get_by_approval(\n                approval.id\n            )\n        )\n\n        if existing_execution is not None:\n            return await self._existing_execution(\n                security=security,\n                incident_id=incident_id,\n                approval=approval,\n                execution=existing_execution,\n                idempotency_key=(\n                    idempotency_key\n                ),\n            )\n\n        incident = await self.incident_store.get(\n            incident_id\n        )\n\n        if incident is None:\n            return ChatOpsWriteOutcome(\n                success=False,\n                status=(\n                    ChatOpsWriteStatus\n                    .WRITE_FAILED\n                ),\n                operation=operation_name,\n                incident_id=incident_id,\n                approval_id=approval.id,\n                operator_id=(\n                    security.principal_id\n                ),\n                failure_code=(\n                    "IncidentNotFound"\n                ),\n                message=(\n                    "Linked Incident could not be loaded for execution"\n                ),\n            )\n\n        try:\n            execution_result = await (\n                self.action_runtime\n                .resume(\n                    approval.id,\n                    incident=incident,\n                    operator_id=(\n                        security.principal_id\n                    ),\n                    idempotency_key=(\n                        idempotency_key\n                    ),\n                )\n            )\n\n        except Exception as exc:\n            return ChatOpsWriteOutcome(\n                success=False,\n                status=(\n                    ChatOpsWriteStatus\n                    .WRITE_FAILED\n                ),\n                operation=operation_name,\n                incident_id=incident_id,\n                approval_id=approval.id,\n                operator_id=(\n                    security.principal_id\n                ),\n                failure_code=(\n                    type(\n                        exc\n                    ).__name__[\n                        :256\n                    ]\n                ),\n                message=(\n                    "Action resume could not complete safely"\n                ),\n            )\n\n        persisted_execution = await (\n            self.action_execution_service\n            .get_by_approval(\n                approval.id\n            )\n        )\n\n        if persisted_execution is None:\n            return ChatOpsWriteOutcome(\n                success=False,\n                status=(\n                    ChatOpsWriteStatus\n                    .WRITE_FAILED\n                ),\n                operation=operation_name,\n                incident_id=incident_id,\n                approval_id=approval.id,\n                operator_id=(\n                    security.principal_id\n                ),\n                failure_code=(\n                    "ActionExecutionNotPersisted"\n                ),\n                message=(\n                    "Action Runtime returned without a durable execution record"\n                ),\n            )\n\n        execution_status = (\n            persisted_execution.status.value\n        )\n\n        verification = await (\n            self.verification_service\n            .get_by_action_execution(\n                persisted_execution.id\n            )\n        )\n\n        if (\n            execution_status\n            == "succeeded"\n            and verification is None\n        ):\n            try:\n                (\n                    verification,\n                    _,\n                ) = await (\n                    self.verification_coordinator\n                    .run(\n                        incident_id=incident_id,\n                        plan=approval.action,\n                        namespace=(\n                            approval.action.namespace\n                        ),\n                        cluster=(\n                            approval.action.cluster\n                        ),\n                        action_execution_id=(\n                            persisted_execution.id\n                        ),\n                        metadata={\n                            **self._audit_metadata(\n                                message=message,\n                                security=security,\n                            ),\n                            "source": "chatops",\n                            "trigger": (\n                                "post_action_execution"\n                            ),\n                            "approval_id": (\n                                approval.id\n                            ),\n                        },\n                    )\n                )\n\n            except Exception as exc:\n                return ChatOpsWriteOutcome(\n                    success=False,\n                    status=(\n                        ChatOpsWriteStatus\n                        .VERIFICATION_FOLLOW_UP_REQUIRED\n                    ),\n                    operation=operation_name,\n                    incident_id=incident_id,\n                    approval_id=approval.id,\n                    execution_id=str(\n                        persisted_execution.id\n                    ),\n                    execution_status=(\n                        execution_status\n                    ),\n                    operator_id=(\n                        security.principal_id\n                    ),\n                    failure_code=(\n                        type(\n                            exc\n                        ).__name__[\n                            :256\n                        ]\n                    ),\n                    message=(\n                        "Action completed but Verification requires follow-up"\n                    ),\n                )\n\n        verification_status = (\n            self._verification_status(\n                verification\n            )\n        )\n\n        result_success = (\n            execution_status\n            == "succeeded"\n            and verification_status\n            == "passed"\n        )\n\n        return ChatOpsWriteOutcome(\n            success=result_success,\n            status=(\n                ChatOpsWriteStatus\n                .EXECUTION_COMPLETED\n                if result_success\n                else (\n                    ChatOpsWriteStatus\n                    .VERIFICATION_FOLLOW_UP_REQUIRED\n                    if execution_status\n                    == "succeeded"\n                    else ChatOpsWriteStatus\n                    .EXECUTION_BLOCKED\n                )\n            ),\n            operation=operation_name,\n            incident_id=incident_id,\n            approval_id=approval.id,\n            execution_id=str(\n                persisted_execution.id\n            ),\n            execution_status=(\n                execution_status\n            ),\n            verification_id=(\n                str(\n                    verification.id\n                )\n                if verification\n                is not None\n                else None\n            ),\n            verification_status=(\n                verification_status\n            ),\n            operator_id=(\n                security.principal_id\n            ),\n            idempotent_replay=bool(\n                execution_result.get(\n                    "idempotent_replay",\n                    False,\n                )\n                if isinstance(\n                    execution_result,\n                    dict,\n                )\n                else False\n            ),\n            message=(\n                "Action and Verification completed"\n                if result_success\n                else (\n                    "Action completed; Verification is not yet passed"\n                    if execution_status\n                    == "succeeded"\n                    else "Action execution did not succeed"\n                )\n            ),\n        )\n\n    async def _existing_execution(\n        self,\n        *,\n        security: ChatOpsSecurityContext,\n        incident_id: str,\n        approval: ApprovalRequest,\n        execution: ActionExecutionRecord,\n        idempotency_key: str,\n    ) -> ChatOpsWriteOutcome:\n        verification = await (\n            self.verification_service\n            .get_by_action_execution(\n                execution.id\n            )\n        )\n\n        verification_status = (\n            self._verification_status(\n                verification\n            )\n        )\n\n        execution_status = (\n            execution.status.value\n        )\n\n        return ChatOpsWriteOutcome(\n            success=(\n                execution_status\n                == "succeeded"\n                and verification_status\n                == "passed"\n            ),\n            status=(\n                ChatOpsWriteStatus\n                .EXISTING_EXECUTION\n            ),\n            operation="action.resume",\n            incident_id=incident_id,\n            approval_id=approval.id,\n            execution_id=str(\n                execution.id\n            ),\n            execution_status=(\n                execution_status\n            ),\n            verification_id=(\n                str(\n                    verification.id\n                )\n                if verification\n                is not None\n                else None\n            ),\n            verification_status=(\n                verification_status\n            ),\n            operator_id=(\n                security.principal_id\n            ),\n            idempotent_replay=(\n                execution.operator_id\n                == security.principal_id\n                and execution.idempotency_key\n                == idempotency_key\n            ),\n            message=(\n                "A durable Action Execution already exists for this Approval"\n            ),\n        )\n\n    @staticmethod\n    def _approval_ambiguous(\n        *,\n        security: ChatOpsSecurityContext,\n        incident_id: str,\n        operation_name: str,\n    ) -> ChatOpsWriteOutcome:\n        return ChatOpsWriteOutcome(\n            success=False,\n            status=(\n                ChatOpsWriteStatus\n                .APPROVAL_AMBIGUOUS\n            ),\n            operation=operation_name,\n            incident_id=incident_id,\n            operator_id=(\n                security.principal_id\n            ),\n            message=(\n                "Multiple Approval candidates exist; ChatOps refuses to guess"\n            ),\n        )\n\n    def _exact_decision_replay(\n        self,\n        *,\n        approvals: list[\n            ApprovalRequest\n        ],\n        message: ChatOpsInboundMessage,\n        security: ChatOpsSecurityContext,\n        desired_status: ApprovalStatus,\n        operation_name: str,\n    ) -> ApprovalRequest | None:\n        matches = []\n\n        for item in approvals:\n            decision = item.decision\n\n            if decision is None:\n                continue\n\n            expected_key = (\n                self._idempotency_key(\n                    message=message,\n                    operation_name=(\n                        operation_name\n                    ),\n                    target_id=item.id,\n                )\n            )\n\n            if (\n                item.status\n                == desired_status\n                and decision.status\n                == desired_status\n                and decision.operator_id\n                == security.principal_id\n                and decision.idempotency_key\n                == expected_key\n            ):\n                matches.append(\n                    item\n                )\n\n        if len(\n            matches\n        ) == 1:\n            return matches[\n                0\n            ]\n\n        return None\n\n    @staticmethod\n    def _idempotency_key(\n        *,\n        message: ChatOpsInboundMessage,\n        operation_name: str,\n        target_id: str,\n    ) -> str:\n        payload = json.dumps(\n            [\n                message.conversation\n                .binding_key(),\n                message.message_id,\n                message.text,\n                operation_name,\n                target_id,\n            ],\n            ensure_ascii=False,\n            separators=(\n                ",",\n                ":",\n            ),\n        )\n\n        return (\n            "chatops:"\n            + hashlib.sha256(\n                payload.encode(\n                    "utf-8"\n                )\n            ).hexdigest()\n        )\n\n    @classmethod\n    def _audit_metadata(\n        cls,\n        *,\n        message: ChatOpsInboundMessage,\n        security: ChatOpsSecurityContext,\n    ) -> dict[str, Any]:\n        metadata = {\n            "source": "chatops",\n            "chatops_binding_key": (\n                message.conversation\n                .binding_key()\n            ),\n            "chatops_message_fingerprint": (\n                hashlib.sha256(\n                    (\n                        message.message_id\n                        + "\\n"\n                        + message.text\n                    ).encode(\n                        "utf-8"\n                    )\n                ).hexdigest()\n            ),\n        }\n\n        metadata.update(\n            security.stable_audit_context()\n        )\n\n        return metadata\n\n    @staticmethod\n    def _decision_reason(\n        text: str,\n    ) -> str:\n        normalized = text.strip()\n\n        if len(\n            normalized\n        ) > 1900:\n            normalized = (\n                normalized[\n                    :1900\n                ]\n            )\n\n        return (\n            "ChatOps decision: "\n            + normalized\n        )\n\n    @staticmethod\n    def _verification_status(\n        verification,\n    ) -> str | None:\n        if verification is None:\n            return None\n\n        status = getattr(\n            verification,\n            "status",\n            None,\n        )\n\n        if status is None:\n            return None\n\n        return str(\n            getattr(\n                status,\n                "value",\n                status,\n            )\n        ).strip().lower()\n\n\n__all__ = [\n    "ChatOpsAuthenticatedWriteBridge",\n    "ChatOpsWriteOutcome",\n    "ChatOpsWriteStatus",\n]\n', 'services/agent_runtime/app/conversation/orchestrator.py': 'from __future__ import annotations\n\nfrom services.agent_runtime.app.conversation.classifier import (\n    DeterministicConversationIntentClassifier,\n)\nfrom services.agent_runtime.app.conversation.models import (\n    ConversationIncidentContext,\n    ConversationIntent,\n    ConversationReplyMode,\n    ConversationReplyPlan,\n    ConversationReplySection,\n    ConversationTurnRequest,\n)\nfrom services.agent_runtime.app.conversation.provider import (\n    BaseConversationIncidentContextProvider,\n)\nfrom services.agent_runtime.app.conversation.store import (\n    InMemoryConversationSessionStore,\n    SQLiteConversationSessionStore,\n)\n\n\nclass ConversationOrchestrator:\n    """\n    Channel-neutral ChatOps core.\n\n    v1 binds a conversation to an Incident, classifies intent, reads a stable\n    Incident projection, and returns a structured reply plan.\n\n    It has no direct Action/Approval/Verification write authority.\n    """\n\n    _WRITE_INTENTS = {\n        ConversationIntent.APPROVE,\n        ConversationIntent.REJECT,\n        ConversationIntent.REMEDIATE,\n    }\n\n    def __init__(\n        self,\n        *,\n        provider: BaseConversationIncidentContextProvider,\n        sessions: (\n            InMemoryConversationSessionStore\n            | SQLiteConversationSessionStore\n            | None\n        ) = None,\n        classifier: (\n            DeterministicConversationIntentClassifier\n            | None\n        ) = None,\n    ) -> None:\n        if not isinstance(\n            provider,\n            BaseConversationIncidentContextProvider,\n        ):\n            raise TypeError(\n                "Conversation context provider is invalid"\n            )\n\n        self.provider = provider\n        self.sessions = (\n            sessions\n            or InMemoryConversationSessionStore()\n        )\n        self.classifier = (\n            classifier\n            or DeterministicConversationIntentClassifier()\n        )\n\n    async def handle(\n        self,\n        request: ConversationTurnRequest,\n    ) -> ConversationReplyPlan:\n        if not isinstance(\n            request,\n            ConversationTurnRequest,\n        ):\n            raise TypeError(\n                "Conversation request is invalid"\n            )\n\n        intent = self.classifier.classify(\n            request.text\n        )\n\n        current = await self.sessions.get(\n            request.conversation_id\n        )\n\n        incident_id = (\n            request.incident_id\n            or (\n                current.incident_id\n                if current is not None\n                else None\n            )\n        )\n\n        await self.sessions.update(\n            conversation_id=request.conversation_id,\n            incident_id=incident_id,\n            intent=intent,\n        )\n\n        if intent == ConversationIntent.HELP:\n            return self._help(\n                request,\n                incident_id,\n            )\n\n        if incident_id is None:\n            return ConversationReplyPlan(\n                conversation_id=request.conversation_id,\n                incident_id=None,\n                intent=intent,\n                mode=ConversationReplyMode.NEEDS_INCIDENT,\n                sections=(\n                    ConversationReplySection(\n                        key="incident_binding",\n                        title="需要 Incident",\n                        lines=(\n                            "请先绑定一个 Incident，再继续查询或操作。",\n                        ),\n                    ),\n                ),\n                suggested_actions=(\n                    "bind_incident",\n                    "help",\n                ),\n            )\n\n        context = await self.provider.get(\n            incident_id\n        )\n\n        if context is None:\n            return ConversationReplyPlan(\n                conversation_id=request.conversation_id,\n                incident_id=incident_id,\n                intent=intent,\n                mode=(\n                    ConversationReplyMode\n                    .INCIDENT_NOT_FOUND\n                ),\n                sections=(\n                    ConversationReplySection(\n                        key="incident",\n                        title="Incident 不存在",\n                        lines=(\n                            f"未找到 Incident {incident_id}。",\n                        ),\n                    ),\n                ),\n                suggested_actions=("bind_incident",),\n            )\n\n        if intent in self._WRITE_INTENTS:\n            return self._write_intent(\n                request=request,\n                context=context,\n                intent=intent,\n            )\n\n        if intent == ConversationIntent.STATUS:\n            return self._status(request, context)\n\n        if intent == ConversationIntent.RCA:\n            return self._rca(request, context)\n\n        if intent == ConversationIntent.EVIDENCE:\n            return self._evidence(request, context)\n\n        if intent == ConversationIntent.NEXT_STEP:\n            return self._next_step(request, context)\n\n        if intent == ConversationIntent.VERIFICATION:\n            return self._verification(request, context)\n\n        return self._unknown(request, context)\n\n    @staticmethod\n    def _base(\n        request,\n        context,\n        *,\n        intent,\n        sections,\n        suggested_actions=(),\n    ):\n        return ConversationReplyPlan(\n            conversation_id=request.conversation_id,\n            incident_id=context.incident_id,\n            intent=intent,\n            mode=ConversationReplyMode.READ_ONLY,\n            sections=tuple(sections),\n            suggested_actions=tuple(suggested_actions),\n        )\n\n    def _status(self, request, context):\n        lines = [f"状态: {context.status}"]\n\n        if context.title:\n            lines.append(f"事件: {context.title}")\n\n        if context.approval_status:\n            lines.append(\n                f"审批: {context.approval_status}"\n            )\n\n        if context.action_execution_status:\n            lines.append(\n                "执行: "\n                + context.action_execution_status\n            )\n\n        if context.verification_status:\n            lines.append(\n                f"验证: {context.verification_status}"\n            )\n\n        return self._base(\n            request,\n            context,\n            intent=ConversationIntent.STATUS,\n            sections=(\n                ConversationReplySection(\n                    key="status",\n                    title="Incident 状态",\n                    lines=tuple(lines),\n                ),\n            ),\n            suggested_actions=(\n                "show_rca",\n                "show_evidence",\n                "what_next",\n            ),\n        )\n\n    def _rca(self, request, context):\n        if context.root_cause:\n            confidence = (\n                f"{context.root_cause_confidence:.0%}"\n                if context.root_cause_confidence is not None\n                else "unknown"\n            )\n            lines = (\n                f"根因: {context.root_cause}",\n                f"置信度: {confidence}",\n            )\n        elif context.hypotheses:\n            best = max(\n                context.hypotheses,\n                key=lambda item: item.confidence,\n            )\n            lines = (\n                "当前尚无最终根因。",\n                f"最高假设: {best.cause}",\n                f"假设置信度: {best.confidence:.0%}",\n            )\n        else:\n            lines = (\n                "当前还没有足够证据形成 RCA。",\n            )\n\n        return self._base(\n            request,\n            context,\n            intent=ConversationIntent.RCA,\n            sections=(\n                ConversationReplySection(\n                    key="rca",\n                    title="根因分析",\n                    lines=lines,\n                ),\n            ),\n            suggested_actions=(\n                "show_evidence",\n                "what_next",\n            ),\n        )\n\n    def _evidence(self, request, context):\n        if not context.evidence:\n            lines = ("当前还没有可展示的证据。",)\n        else:\n            lines = tuple(\n                (\n                    ("✓ " if item.trusted else "△ ")\n                    + item.summary\n                    + f" [{item.source}]"\n                )\n                for item in context.evidence\n            )\n\n        return self._base(\n            request,\n            context,\n            intent=ConversationIntent.EVIDENCE,\n            sections=(\n                ConversationReplySection(\n                    key="evidence",\n                    title="证据",\n                    lines=lines,\n                ),\n            ),\n            suggested_actions=(\n                "show_rca",\n                "what_next",\n            ),\n        )\n\n    def _next_step(self, request, context):\n        lines = []\n\n        if context.recommended_action:\n            lines.append(\n                f"建议: {context.recommended_action}"\n            )\n\n            if context.action_risk:\n                lines.append(\n                    f"风险: {context.action_risk}"\n                )\n\n            if context.approval_status:\n                lines.append(\n                    f"审批状态: {context.approval_status}"\n                )\n        elif context.root_cause:\n            lines.append(\n                "根因已经形成，但当前没有可执行修复建议。"\n            )\n        else:\n            lines.append(\n                "继续收集证据并缩小根因假设。"\n            )\n\n        return self._base(\n            request,\n            context,\n            intent=ConversationIntent.NEXT_STEP,\n            sections=(\n                ConversationReplySection(\n                    key="next_step",\n                    title="下一步",\n                    lines=tuple(lines),\n                ),\n            ),\n            suggested_actions=(\n                (\n                    "request_remediation"\n                    if context.recommended_action\n                    else "show_evidence"\n                ),\n            ),\n        )\n\n    def _verification(self, request, context):\n        lines = (\n            (\n                f"验证状态: {context.verification_status}"\n                if context.verification_status\n                else "当前还没有 Verification 结果。"\n            ),\n        )\n\n        return self._base(\n            request,\n            context,\n            intent=ConversationIntent.VERIFICATION,\n            sections=(\n                ConversationReplySection(\n                    key="verification",\n                    title="恢复验证",\n                    lines=lines,\n                ),\n            ),\n            suggested_actions=("show_status",),\n        )\n\n    @staticmethod\n    def _write_intent(\n        *,\n        request,\n        context,\n        intent,\n    ):\n        operation = {\n            ConversationIntent.APPROVE: "approval.approve",\n            ConversationIntent.REJECT: "approval.reject",\n            ConversationIntent.REMEDIATE: "action.resume",\n        }[intent]\n\n        return ConversationReplyPlan(\n            conversation_id=request.conversation_id,\n            incident_id=context.incident_id,\n            intent=intent,\n            mode=(\n                ConversationReplyMode\n                .WRITE_ACTION_REQUIRED\n            ),\n            sections=(\n                ConversationReplySection(\n                    key="write_boundary",\n                    title="需要认证写操作",\n                    lines=(\n                        "Conversation Orchestrator v1 不直接执行写操作。",\n                        "该意图必须通过现有认证、RBAC、Approval/Action 边界继续。",\n                    ),\n                ),\n            ),\n            suggested_actions=(\n                "open_authenticated_write_flow",\n                "show_status",\n            ),\n            write_operation=operation,\n        )\n\n    @staticmethod\n    def _help(request, incident_id):\n        return ConversationReplyPlan(\n            conversation_id=request.conversation_id,\n            incident_id=incident_id,\n            intent=ConversationIntent.HELP,\n            mode=ConversationReplyMode.READ_ONLY,\n            sections=(\n                ConversationReplySection(\n                    key="help",\n                    title="可以这样问",\n                    lines=(\n                        "现在状态怎么样？",\n                        "根因是什么？",\n                        "有哪些证据？",\n                        "下一步怎么办？",\n                        "验证结果怎么样？",\n                        "帮我修一下。",\n                        "批准执行。",\n                    ),\n                ),\n            ),\n        )\n\n    def _unknown(self, request, context):\n        return self._base(\n            request,\n            context,\n            intent=ConversationIntent.UNKNOWN,\n            sections=(\n                ConversationReplySection(\n                    key="unknown",\n                    title="我还不能确定你的意图",\n                    lines=(\n                        "可以询问状态、根因、证据、下一步或验证结果。",\n                    ),\n                ),\n            ),\n            suggested_actions=("help",),\n        )\n', 'services/agent_runtime/app/conversation/__init__.py': 'from services.agent_runtime.app.conversation.chatops import (\n    BaseChatOpsChannelAdapter,\n    ChatOpsConversationGateway,\n    ChatOpsConversationRef,\n    ChatOpsInboundMessage,\n    ChatOpsOutboundMessage,\n)\nfrom services.agent_runtime.app.conversation.classifier import (\n    DeterministicConversationIntentClassifier,\n)\nfrom services.agent_runtime.app.conversation.identity import (\n    BaseChatOpsActorVerifier,\n    ChatOpsActorVerificationError,\n    ChatOpsAuthorizationDeniedError,\n    ChatOpsIdentityAuthenticationError,\n    ChatOpsIdentityBinding,\n    ChatOpsIdentityBindingError,\n    ChatOpsIdentityBindingRegistry,\n    ChatOpsIdentityAuthenticator,\n    ChatOpsIdentityError,\n    ChatOpsSecurityAdapter,\n    ChatOpsSecurityContext,\n    ChatOpsVerifiedActor,\n)\nfrom services.agent_runtime.app.conversation.models import (\n    ConversationEvidenceView,\n    ConversationHypothesisView,\n    ConversationIncidentContext,\n    ConversationIntent,\n    ConversationReplyMode,\n    ConversationReplyPlan,\n    ConversationReplySection,\n    ConversationSession,\n    ConversationTurnRequest,\n)\nfrom services.agent_runtime.app.conversation.orchestrator import (\n    ConversationOrchestrator,\n)\nfrom services.agent_runtime.app.conversation.provider import (\n    BaseConversationIncidentContextProvider,\n    DictConversationIncidentContextProvider,\n)\nfrom services.agent_runtime.app.conversation.runtime_provider import (\n    RuntimeConversationIncidentContextProvider,\n)\nfrom services.agent_runtime.app.conversation.store import (\n    InMemoryConversationSessionStore,\n    SQLiteConversationSessionStore,\n)\nfrom services.agent_runtime.app.conversation.write_bridge import (\n    ChatOpsAuthenticatedWriteBridge,\n    ChatOpsWriteOutcome,\n    ChatOpsWriteStatus,\n)\n\n\n__all__ = [\n    "BaseChatOpsActorVerifier",\n    "BaseChatOpsChannelAdapter",\n    "ChatOpsActorVerificationError",\n    "ChatOpsAuthenticatedWriteBridge",\n    "ChatOpsAuthorizationDeniedError",\n    "BaseConversationIncidentContextProvider",\n    "ChatOpsConversationGateway",\n    "ChatOpsConversationRef",\n    "ChatOpsIdentityAuthenticationError",\n    "ChatOpsIdentityBinding",\n    "ChatOpsIdentityBindingError",\n    "ChatOpsIdentityBindingRegistry",\n    "ChatOpsIdentityAuthenticator",\n    "ChatOpsIdentityError",\n    "ChatOpsInboundMessage",\n    "ChatOpsOutboundMessage",\n    "ChatOpsSecurityAdapter",\n    "ChatOpsSecurityContext",\n    "ChatOpsVerifiedActor",\n    "ChatOpsWriteOutcome",\n    "ChatOpsWriteStatus",\n    "ConversationEvidenceView",\n    "ConversationHypothesisView",\n    "ConversationIncidentContext",\n    "ConversationIntent",\n    "ConversationOrchestrator",\n    "ConversationReplyMode",\n    "ConversationReplyPlan",\n    "ConversationReplySection",\n    "ConversationSession",\n    "ConversationTurnRequest",\n    "DeterministicConversationIntentClassifier",\n    "DictConversationIncidentContextProvider",\n    "InMemoryConversationSessionStore",\n    "RuntimeConversationIncidentContextProvider",\n    "SQLiteConversationSessionStore",\n]\n', 'services/agent_runtime/app/runtime/runtime.py': 'from copy import deepcopy\nfrom typing import Any\n\nfrom services.agent_runtime.app.registry.factory import (\n    create_agent_registry,\n)\nfrom services.agent_runtime.app.llm.gateway.factory import (\n    create_llm_gateway,\n)\nfrom services.agent_runtime.app.llm.gateway.gateway import (\n    LLMGateway,\n)\nfrom services.agent_runtime.app.planner.agent_planner import (\n    AgentPlanner,\n)\nfrom services.agent_runtime.app.pipeline.planner_pipeline import (\n    PlannerPipeline,\n)\nfrom services.agent_runtime.app.memory.store import (\n    MemoryStore,\n)\nfrom services.agent_runtime.app.tools.factory import (\n    create_tool_manager,\n)\nfrom services.agent_runtime.app.tools.kubernetes.router import (\n    KubernetesClusterRegistry,\n)\nfrom services.agent_runtime.app.tools.kubernetes.connection_factory import (\n    create_kubernetes_cluster_registry,\n)\nfrom services.agent_runtime.app.tools.prometheus.router import (\n    PrometheusClusterRegistry,\n)\nfrom services.agent_runtime.app.tools.prometheus.connection_factory import (\n    create_prometheus_cluster_registry,\n)\nfrom services.agent_runtime.app.skills.factory import (\n    create_skill_registry,\n)\nfrom services.agent_runtime.app.mcp.factory import (\n    create_mcp_registry,\n)\nfrom services.agent_runtime.app.observability.collector import (\n    TraceCollector,\n)\nfrom services.agent_runtime.app.evaluation.factory import (\n    create_evaluation_registry,\n)\nfrom services.agent_runtime.app.policy.factory import (\n    create_policy_engine,\n)\nfrom services.agent_runtime.app.approval.service import (\n    ApprovalService,\n)\nfrom services.agent_runtime.app.incident.store import (\n    IncidentStore,\n)\nfrom services.agent_runtime.app.incident.service import (\n    IncidentService,\n)\nfrom services.agent_runtime.app.investigation.comparison import (\n    build_rca_investigation_comparison,\n)\nfrom services.agent_runtime.app.investigation.factory import (\n    create_investigation_coordinator,\n)\nfrom services.agent_runtime.app.investigation.llm_gateway_adapter import (\n    InvestigationLLMGatewayAdapter,\n)\nfrom services.agent_runtime.app.investigation.reasoner import (\n    BaseInvestigationReasoner,\n    LLMInvestigationReasoner,\n)\nfrom services.agent_runtime.app.investigation.settings import (\n    InvestigationSettings,\n)\nfrom services.agent_runtime.app.investigation.multi_cluster_readiness import (\n    ProductionMultiClusterReadinessError,\n    ProductionMultiClusterReadinessGate,\n)\nfrom services.agent_runtime.app.investigation.live_readiness import (\n    ProductionReadinessLiveProbe,\n    ProductionReadinessLiveProbeError,\n)\nfrom services.agent_runtime.app.investigation.models import (\n    InvestigationState,\n)\nfrom services.agent_runtime.app.investigation.persistence_models import (\n    build_incident_analysis_record,\n)\nfrom services.agent_runtime.app.investigation.store import (\n    IncidentAnalysisStore,\n)\nfrom services.agent_runtime.app.conversation.chatops import (\n    ChatOpsConversationGateway,\n)\nfrom services.agent_runtime.app.conversation.identity import (\n    BaseChatOpsActorVerifier,\n    ChatOpsIdentityBindingRegistry,\n    ChatOpsIdentityAuthenticator,\n    ChatOpsSecurityAdapter,\n)\nfrom services.agent_runtime.app.conversation.orchestrator import (\n    ConversationOrchestrator,\n)\nfrom services.agent_runtime.app.conversation.runtime_provider import (\n    RuntimeConversationIncidentContextProvider,\n)\nfrom services.agent_runtime.app.conversation.store import (\n    SQLiteConversationSessionStore,\n)\nfrom services.agent_runtime.app.conversation.write_bridge import (\n    ChatOpsAuthenticatedWriteBridge,\n)\nfrom services.agent_runtime.app.model.context import (\n    AgentContext,\n)\nfrom services.agent_runtime.app.workflow.service import (\n    WorkflowService,\n)\nfrom services.agent_runtime.app.action.execution_service import (\n    ActionExecutionService,\n)\nfrom services.agent_runtime.app.action.execution_store import (\n    ActionExecutionStore,\n)\nfrom services.agent_runtime.app.action.kubernetes_preflight import (\n    KubernetesPreflightResolver,\n)\nfrom services.agent_runtime.app.action.kubernetes_preflight_factory import (\n    create_kubernetes_preflight_resolver,\n)\nfrom services.agent_runtime.app.action.kubernetes_production_executor import (\n    KubernetesProductionExecutor,\n)\nfrom services.agent_runtime.app.action.kubernetes_production_factory import (\n    create_kubernetes_production_executor,\n)\nfrom services.agent_runtime.app.action.preflight_artifact_service import (\n    PreflightArtifactService,\n)\nfrom services.agent_runtime.app.action.preflight_artifact_store import (\n    PreflightArtifactStore,\n)\nfrom services.agent_runtime.app.action.production_action_preparation import (\n    ProductionActionPreparationService,\n)\nfrom services.agent_runtime.app.action.production_action_query import (\n    ProductionActionQueryService,\n)\nfrom services.agent_runtime.app.action.production_action_guard import (\n    ProductionActionExpiryGuard,\n)\nfrom services.agent_runtime.app.action.production_pilot import (\n    KubernetesProductionPilotControl,\n    ProductionPilotReadinessService,\n)\nfrom services.agent_runtime.app.action.production_pilot_factory import (\n    create_kubernetes_production_pilot_control,\n)\nfrom services.agent_runtime.app.action.production_pilot_budget_service import (\n    ProductionPilotBudgetService,\n)\nfrom services.agent_runtime.app.action.production_pilot_budget_store import (\n    ProductionPilotBudgetStore,\n)\nfrom services.agent_runtime.app.action.production_pilot_rehearsal import (\n    ProductionPilotRehearsalService,\n)\nfrom services.agent_runtime.app.action.production_pilot_crash_rehearsal import (\n    ProductionPilotCrashRecoveryRehearsalService,\n)\nfrom services.agent_runtime.app.action.production_pilot_pre_enable_evidence import (\n    ProductionPilotPreEnableEvidenceService,\n)\nfrom services.agent_runtime.app.action.production_pilot_final_handoff import (\n    ProductionPilotFinalHandoffRehearsalService,\n)\nfrom services.agent_runtime.app.action.production_pilot_live_probe import (\n    ProductionPilotLiveReadinessProbe,\n    create_production_pilot_live_readiness_probe,\n)\nfrom services.agent_runtime.app.action.production_pilot_go_no_go_service import (\n    ProductionPilotGoNoGoService,\n)\nfrom services.agent_runtime.app.action.production_pilot_go_no_go_store import (\n    ProductionPilotGoNoGoStore,\n)\nfrom services.agent_runtime.app.action.production_pilot_ceremony_service import (\n    ProductionPilotCeremonyService,\n)\nfrom services.agent_runtime.app.action.production_pilot_ceremony_store import (\n    ProductionPilotCeremonyStore,\n)\nfrom services.agent_runtime.app.verification.collector import (\n    VerificationEvidenceCollector,\n)\nfrom services.agent_runtime.app.verification.coordinator import (\n    VerificationCoordinator,\n)\nfrom services.agent_runtime.app.verification.profiles import (\n    VerificationProfileFactory,\n)\nfrom services.agent_runtime.app.verification.service import (\n    VerificationService,\n)\nfrom services.agent_runtime.app.verification.store import (\n    VerificationStore,\n)\nfrom services.agent_runtime.app.runtime.action_runtime import (\n    ActionRuntime,\n)\nfrom services.agent_runtime.app.runtime.verification_runtime import (\n    VerificationRuntime,\n)\nfrom services.agent_runtime.app.security.factory import (\n    create_authentication_service,\n)\nfrom services.agent_runtime.app.security.policy import (\n    SecurityPolicyEngine,\n)\nfrom services.agent_runtime.app.security.service import (\n    AuthenticationService,\n)\nfrom services.sandbox.executor.local import (\n    LocalSandboxExecutor,\n)\nfrom services.sandbox.policy.validator import (\n    SandboxPolicyValidator,\n)\n\n\nfrom services.agent_runtime.app.incident_evidence.recorder import (\n    ProductionIncidentEvidenceRecorder,\n)\nfrom services.agent_runtime.app.incident_evidence.settings import (\n    IncidentEvidenceRecorderSettings,\n)\n\nclass AgentRuntime:\n    """\n    Runtime container.\n\n    Owns and shares security and runtime infrastructure\n    across Pipeline, Action and Verification.\n\n    security_policy is the RBAC authorization policy. The existing policy\n    attribute remains the remediation business policy engine.\n    """\n\n    def __init__(\n        self,\n        authentication_service: (\n            AuthenticationService | None\n        ) = None,\n        security_policy: (\n            SecurityPolicyEngine | None\n        ) = None,\n        kubernetes_preflight: (\n            KubernetesPreflightResolver | None\n        ) = None,\n        kubernetes_production_executor: (\n            KubernetesProductionExecutor | None\n        ) = None,\n        production_pilot_control: (\n            KubernetesProductionPilotControl | None\n        ) = None,\n        production_pilot_budget_service: (\n            ProductionPilotBudgetService | None\n        ) = None,\n        production_pilot_live_probe: (\n            ProductionPilotLiveReadinessProbe | None\n        ) = None,\n        kubernetes_cluster_registry: (\n            KubernetesClusterRegistry | None\n        ) = None,\n        prometheus_cluster_registry: (\n            PrometheusClusterRegistry | None\n        ) = None,\n        llm_gateway: (\n            LLMGateway | None\n        ) = None,\n        investigation_reasoner: (\n            BaseInvestigationReasoner | None\n        ) = None,\n        investigation_settings: (\n            InvestigationSettings | None\n        ) = None,\n    ) -> None:\n        # Validate every injected security component before factories, stores\n        # or other runtime components can produce side effects.\n        if (\n            authentication_service is not None\n            and not isinstance(\n                authentication_service,\n                AuthenticationService,\n            )\n        ):\n            raise TypeError(\n                "AgentRuntime authentication service is invalid"\n            )\n\n        if (\n            security_policy is not None\n            and not isinstance(\n                security_policy,\n                SecurityPolicyEngine,\n            )\n        ):\n            raise TypeError(\n                "AgentRuntime security policy is invalid"\n            )\n\n        if (\n            kubernetes_preflight is not None\n            and not isinstance(\n                kubernetes_preflight,\n                KubernetesPreflightResolver,\n            )\n        ):\n            raise TypeError(\n                "AgentRuntime Kubernetes preflight resolver is invalid"\n            )\n\n        if (\n            kubernetes_production_executor is not None\n            and not isinstance(\n                kubernetes_production_executor,\n                KubernetesProductionExecutor,\n            )\n        ):\n            raise TypeError(\n                "AgentRuntime Kubernetes production executor is invalid"\n            )\n\n        if (\n            production_pilot_control is not None\n            and not isinstance(\n                production_pilot_control,\n                KubernetesProductionPilotControl,\n            )\n        ):\n            raise TypeError(\n                "AgentRuntime Kubernetes production pilot control is invalid"\n            )\n\n        if (\n            production_pilot_budget_service is not None\n            and not isinstance(\n                production_pilot_budget_service,\n                ProductionPilotBudgetService,\n            )\n        ):\n            raise TypeError(\n                "AgentRuntime Kubernetes production pilot budget service is invalid"\n            )\n\n        if (\n            production_pilot_live_probe is not None\n            and not isinstance(\n                production_pilot_live_probe,\n                ProductionPilotLiveReadinessProbe,\n            )\n        ):\n            raise TypeError(\n                "AgentRuntime Production Pilot live probe is invalid"\n            )\n\n        if (\n            kubernetes_cluster_registry is not None\n            and not isinstance(\n                kubernetes_cluster_registry,\n                KubernetesClusterRegistry,\n            )\n        ):\n            raise TypeError(\n                "AgentRuntime Kubernetes cluster registry is invalid"\n            )\n\n        if (\n            prometheus_cluster_registry is not None\n            and not isinstance(\n                prometheus_cluster_registry,\n                PrometheusClusterRegistry,\n            )\n        ):\n            raise TypeError(\n                "AgentRuntime Prometheus cluster registry is invalid"\n            )\n\n        if (\n            llm_gateway is not None\n            and not isinstance(\n                llm_gateway,\n                LLMGateway,\n            )\n        ):\n            raise TypeError(\n                "AgentRuntime shared LLM gateway is invalid"\n            )\n\n        if (\n            investigation_reasoner is not None\n            and not isinstance(\n                investigation_reasoner,\n                BaseInvestigationReasoner,\n            )\n        ):\n            raise TypeError(\n                "AgentRuntime Investigation reasoner is invalid"\n            )\n\n        if (\n            investigation_settings is not None\n            and not isinstance(\n                investigation_settings,\n                InvestigationSettings,\n            )\n        ):\n            raise TypeError(\n                "AgentRuntime Investigation settings are invalid"\n            )\n\n        # Resolve disabled-default Investigation configuration before any\n        # Runtime store, tool, credential, network or LLM component is created.\n        self.investigation_settings = (\n            investigation_settings\n            if investigation_settings is not None\n            else InvestigationSettings.from_environment()\n        )\n\n        investigation_shared_gateway = None\n\n        # An enabled LLM-backed Investigation must use the exact shared\n        # LLMGateway instance that AgentRuntime will provide to its Agents.\n        #\n        # Disabled Investigation deliberately does not inspect or touch the\n        # supplied reasoner\'s LLM adapter.\n        if (\n            self.investigation_settings.enabled\n            and isinstance(\n                investigation_reasoner,\n                LLMInvestigationReasoner,\n            )\n        ):\n            investigation_llm = (\n                investigation_reasoner.investigation_llm\n            )\n\n            if not isinstance(\n                investigation_llm,\n                InvestigationLLMGatewayAdapter,\n            ):\n                raise TypeError(\n                    "AgentRuntime LLM Investigation requires "\n                    "InvestigationLLMGatewayAdapter"\n                )\n\n            investigation_shared_gateway = (\n                investigation_llm.llm_gateway\n            )\n\n            if not isinstance(\n                investigation_shared_gateway,\n                LLMGateway,\n            ):\n                raise TypeError(\n                    "AgentRuntime Investigation shared LLM gateway is invalid"\n                )\n\n            if (\n                llm_gateway is not None\n                and investigation_shared_gateway\n                is not llm_gateway\n            ):\n                raise TypeError(\n                    "AgentRuntime Investigation LLM gateway must be shared"\n                )\n\n        # Preserve the existing fail-closed Investigation assembly boundary.\n        # Enabled mode without an explicit reasoner still fails here before\n        # any Runtime or LLM infrastructure is constructed.\n        self.investigation_coordinator = (\n            create_investigation_coordinator(\n                reasoner=investigation_reasoner,\n                settings=self.investigation_settings,\n            )\n        )\n\n        # Do not construct a default Gateway yet. Keeping this unresolved\n        # preserves the previous initialization order. If Investigation\n        # already carries the approved Gateway Adapter, Runtime adopts that\n        # exact Gateway object as its shared instance.\n        self.llm_gateway = (\n            llm_gateway\n            if llm_gateway is not None\n            else investigation_shared_gateway\n        )\n\n        self.authentication = (\n            authentication_service\n            if authentication_service is not None\n            else create_authentication_service()\n        )\n\n        self.security_policy = (\n            security_policy\n            if security_policy is not None\n            else SecurityPolicyEngine()\n        )\n\n        self.kubernetes_preflight = (\n            kubernetes_preflight\n            if kubernetes_preflight is not None\n            else create_kubernetes_preflight_resolver()\n        )\n\n        self.production_pilot_control = (\n            production_pilot_control\n            if production_pilot_control is not None\n            else create_kubernetes_production_pilot_control()\n        )\n\n        # This independent gate may read both credential values at startup,\n        # but can construct only a two-GET probe. Disabled mode returns before\n        # any credential or CA access.\n        self.production_pilot_live_probe = (\n            production_pilot_live_probe\n            if production_pilot_live_probe is not None\n            else create_production_pilot_live_readiness_probe()\n        )\n\n        self.production_pilot_budget_store = None\n        self.production_pilot_budget_service = (\n            production_pilot_budget_service\n        )\n        if (\n            self.production_pilot_budget_service is None\n            and self.production_pilot_control.config.enabled\n        ):\n            self.production_pilot_budget_store = (\n                ProductionPilotBudgetStore()\n            )\n            self.production_pilot_budget_service = (\n                ProductionPilotBudgetService(\n                    store=(\n                        self.production_pilot_budget_store\n                    )\n                )\n            )\n\n        self.kubernetes_production_executor = (\n            kubernetes_production_executor\n            if kubernetes_production_executor is not None\n            else create_kubernetes_production_executor(\n                pilot_control=(\n                    self.production_pilot_control\n                ),\n                pilot_budget_service=(\n                    self.production_pilot_budget_service\n                ),\n            )\n        )\n\n        if self.kubernetes_production_executor is not None:\n            executor_control = getattr(\n                self.kubernetes_production_executor,\n                "pilot_control",\n                None,\n            )\n            if executor_control is None:\n                self.kubernetes_production_executor.pilot_control = (\n                    self.production_pilot_control\n                )\n            elif executor_control is not self.production_pilot_control:\n                raise TypeError(\n                    "AgentRuntime Kubernetes production pilot control must be shared"\n                )\n            executor_budget = getattr(\n                self.kubernetes_production_executor,\n                "pilot_budget_service",\n                None,\n            )\n            if executor_budget is None:\n                if self.production_pilot_budget_service is None:\n                    raise TypeError(\n                        "AgentRuntime Kubernetes production pilot budget is unavailable"\n                    )\n                self.kubernetes_production_executor.pilot_budget_service = (\n                    self.production_pilot_budget_service\n                )\n            elif executor_budget is not self.production_pilot_budget_service:\n                raise TypeError(\n                    "AgentRuntime Kubernetes production pilot budget must be shared"\n                )\n\n        if (\n            self.kubernetes_production_executor is not None\n            and self.kubernetes_preflight is None\n        ):\n            raise TypeError(\n                "AgentRuntime Kubernetes production executor requires "\n                "trusted preflight"\n            )\n\n        self.production_pilot_readiness = (\n            ProductionPilotReadinessService(\n                control=(\n                    self.production_pilot_control\n                ),\n                production_executor_configured=(\n                    self.kubernetes_production_executor\n                    is not None\n                ),\n            )\n        )\n        self.production_pilot_rehearsal = (\n            ProductionPilotRehearsalService(\n                control=(\n                    self.production_pilot_control\n                ),\n                budget_service=(\n                    self.production_pilot_budget_service\n                ),\n                production_executor_configured=(\n                    self.kubernetes_production_executor\n                    is not None\n                ),\n            )\n        )\n        # Pure recovery-policy proof. It owns no store, credential, network\n        # client or executor and is available while the production gate is\n        # disabled so operators can rehearse recovery before enablement.\n        self.production_pilot_crash_recovery_rehearsal = (\n            ProductionPilotCrashRecoveryRehearsalService()\n        )\n\n        self.memory = MemoryStore()\n\n        if (\n            kubernetes_cluster_registry\n            is None\n        ):\n            self.kubernetes_cluster_registry = (\n                create_kubernetes_cluster_registry()\n            )\n        else:\n            self.kubernetes_cluster_registry = (\n                kubernetes_cluster_registry\n            )\n\n        if (\n            prometheus_cluster_registry\n            is None\n        ):\n            self.prometheus_cluster_registry = (\n                create_prometheus_cluster_registry()\n            )\n        else:\n            self.prometheus_cluster_registry = (\n                prometheus_cluster_registry\n            )\n\n        self.cluster_verified_evidence_required = (\n            self.kubernetes_cluster_registry\n            is not None\n            or self.prometheus_cluster_registry\n            is not None\n        )\n\n        if (\n            self.investigation_coordinator\n            is not None\n        ):\n            self.investigation_coordinator.require_cluster_verified_evidence = (\n                self.cluster_verified_evidence_required\n            )\n\n        tool_manager_kwargs = {}\n\n        if (\n            self.kubernetes_cluster_registry\n            is not None\n        ):\n            tool_manager_kwargs[\n                "kubernetes_cluster_registry"\n            ] = self.kubernetes_cluster_registry\n\n        if (\n            self.prometheus_cluster_registry\n            is not None\n        ):\n            tool_manager_kwargs[\n                "prometheus_cluster_registry"\n            ] = self.prometheus_cluster_registry\n\n        if tool_manager_kwargs:\n            self.tools = create_tool_manager(\n                **tool_manager_kwargs\n            )\n        else:\n            self.tools = create_tool_manager()\n\n        readiness_registry_types_valid = (\n            (\n                self.kubernetes_cluster_registry\n                is None\n                or isinstance(\n                    self.kubernetes_cluster_registry,\n                    KubernetesClusterRegistry,\n                )\n            )\n            and (\n                self.prometheus_cluster_registry\n                is None\n                or isinstance(\n                    self.prometheus_cluster_registry,\n                    PrometheusClusterRegistry,\n                )\n            )\n        )\n\n        self.production_multi_cluster_readiness = None\n        self.production_multi_cluster_coverage = None\n\n        self.production_multi_cluster_live_readiness = None\n\n        if readiness_registry_types_valid:\n            self.production_multi_cluster_readiness = (\n                ProductionMultiClusterReadinessGate(\n                    kubernetes_cluster_registry=(\n                        self.kubernetes_cluster_registry\n                    ),\n                    prometheus_cluster_registry=(\n                        self.prometheus_cluster_registry\n                    ),\n                    tools=self.tools,\n                    strict_evidence_required=(\n                        self.cluster_verified_evidence_required\n                    ),\n                )\n            )\n\n            self.production_multi_cluster_coverage = (\n                self.production_multi_cluster_readiness\n                .evaluate_all()\n            )\n\n            if (\n                self.production_multi_cluster_readiness\n                .applicable\n            ):\n                self.production_multi_cluster_live_readiness = (\n                    ProductionReadinessLiveProbe(\n                        readiness_gate=(\n                            self.production_multi_cluster_readiness\n                        ),\n                        tools=self.tools,\n                    )\n                )\n\n        self.skills = create_skill_registry()\n        self.mcp = create_mcp_registry()\n        self.tracer = TraceCollector()\n        self.evaluators = create_evaluation_registry()\n\n        # Remediation business policy. This is intentionally separate from\n        # security_policy, which authorizes operator-facing operations.\n        self.policy = create_policy_engine()\n\n        self.preflight_artifact_store = None\n        self.preflight_artifact_service = None\n        self.production_action_guard = None\n        self.production_action_preparation = None\n        self.production_action_query = None\n\n        if self.kubernetes_preflight is not None:\n            self.preflight_artifact_store = PreflightArtifactStore()\n            self.preflight_artifact_service = PreflightArtifactService(\n                store=self.preflight_artifact_store\n            )\n            self.production_action_guard = (\n                ProductionActionExpiryGuard(\n                    artifact_service=(\n                        self.preflight_artifact_service\n                    )\n                )\n            )\n\n        self.approval = ApprovalService()\n\n        if self.production_action_guard is not None:\n            self.approval.manager.set_transition_guard(\n                self.production_action_guard\n            )\n\n        if self.preflight_artifact_service is not None:\n            self.production_action_preparation = (\n                ProductionActionPreparationService(\n                    resolver=self.kubernetes_preflight,\n                    artifact_service=self.preflight_artifact_service,\n                    approval_service=self.approval,\n                )\n            )\n\n        self.production_pilot_ceremony_store = None\n        self.production_pilot_ceremony = None\n        if (\n            self.production_pilot_control.config.enabled\n            and self.production_pilot_budget_service is not None\n            and self.preflight_artifact_service is not None\n        ):\n            self.production_pilot_ceremony_store = (\n                ProductionPilotCeremonyStore()\n            )\n            self.production_pilot_ceremony = (\n                ProductionPilotCeremonyService(\n                    store=(\n                        self.production_pilot_ceremony_store\n                    ),\n                    control=(\n                        self.production_pilot_control\n                    ),\n                    rehearsal=(\n                        self.production_pilot_rehearsal\n                    ),\n                    budget_service=(\n                        self.production_pilot_budget_service\n                    ),\n                    approval_service=self.approval,\n                    artifact_service=(\n                        self.preflight_artifact_service\n                    ),\n                )\n            )\n\n        self.incident_store = IncidentStore()\n\n        if self.preflight_artifact_service is not None:\n            self.production_action_query = (\n                ProductionActionQueryService(\n                    artifact_service=(\n                        self.preflight_artifact_service\n                    ),\n                    approval_service=self.approval,\n                    incident_store=self.incident_store,\n                )\n            )\n\n        self.incident_service = IncidentService(\n            store=self.incident_store\n        )\n\n        self.workflow_service = WorkflowService(\n            incident_service=self.incident_service\n        )\n\n        self.action_execution_store = ActionExecutionStore()\n\n        self.action_execution_service = ActionExecutionService(\n            store=self.action_execution_store\n        )\n\n        self.action_runtime = ActionRuntime(\n            approval_service=self.approval,\n            incident_store=self.incident_store,\n            action_execution_service=self.action_execution_service,\n            production_action_guard=(\n                self.production_action_guard\n            ),\n            kubernetes_production_executor=(\n                self.kubernetes_production_executor\n            ),\n            preflight_artifact_service=(\n                self.preflight_artifact_service\n                if self.kubernetes_production_executor is not None\n                else None\n            ),\n            production_pilot_control=(\n                self.production_pilot_control\n            ),\n            production_pilot_budget_service=(\n                self.production_pilot_budget_service\n            ),\n            production_pilot_ceremony_service=(\n                self.production_pilot_ceremony\n                if self.kubernetes_production_executor is not None\n                else None\n            ),\n        )\n\n        self.verification_store = VerificationStore()\n\n        self.verification = VerificationService(\n            store=self.verification_store\n        )\n\n        self.verification_runtime = VerificationRuntime(\n            verification_service=self.verification,\n            incident_store=self.incident_store,\n        )\n\n        self.verification_profile_factory = VerificationProfileFactory()\n\n        self.verification_collector = VerificationEvidenceCollector(\n            tools=self.tools,\n            require_cluster_verified_evidence=(\n                self.cluster_verified_evidence_required\n            ),\n        )\n\n        self.verification_coordinator = VerificationCoordinator(\n            profile_factory=self.verification_profile_factory,\n            collector=self.verification_collector,\n            verification_runtime=self.verification_runtime,\n        )\n\n        self.incident_analysis_store = (\n            IncidentAnalysisStore()\n        )\n\n        self.conversation_context_provider = (\n            RuntimeConversationIncidentContextProvider(\n                incident_store=self.incident_store,\n                analysis_store=(\n                    self.incident_analysis_store\n                ),\n                approval_service=self.approval,\n                action_execution_service=(\n                    self.action_execution_service\n                ),\n                verification_service=(\n                    self.verification\n                ),\n            )\n        )\n\n        self.conversation_sessions = (\n            SQLiteConversationSessionStore()\n        )\n\n        self.conversation = ConversationOrchestrator(\n            provider=(\n                self.conversation_context_provider\n            ),\n            sessions=(\n                self.conversation_sessions\n            ),\n        )\n\n        self.chatops = ChatOpsConversationGateway(\n            orchestrator=self.conversation\n        )\n\n        # Final pre-enable evidence is assembled only when every production\n        # preparation component is available. The service is read-only and\n        # deliberately owns no executor or mutable workflow operation.\n        self.production_pilot_pre_enable_evidence = None\n        if all(\n            component is not None\n            for component in (\n                self.production_pilot_ceremony,\n                self.production_pilot_budget_service,\n                self.preflight_artifact_service,\n            )\n        ):\n            self.production_pilot_pre_enable_evidence = (\n                ProductionPilotPreEnableEvidenceService(\n                    readiness_service=(\n                        self.production_pilot_readiness\n                    ),\n                    rehearsal_service=(\n                        self.production_pilot_rehearsal\n                    ),\n                    crash_rehearsal_service=(\n                        self.production_pilot_crash_recovery_rehearsal\n                    ),\n                    ceremony_service=(\n                        self.production_pilot_ceremony\n                    ),\n                    budget_service=(\n                        self.production_pilot_budget_service\n                    ),\n                    artifact_service=(\n                        self.preflight_artifact_service\n                    ),\n                    approval_service=self.approval,\n                    incident_store=self.incident_store,\n                    action_execution_service=(\n                        self.action_execution_service\n                    ),\n                    verification_service=self.verification,\n                )\n            )\n\n        # The final handoff rehearsal is also strictly read-only. It is\n        # available only with the full prepared Pilot chain and explicitly\n        # records whether production executors remain absent while the gate\n        # is disabled.\n        self.production_pilot_final_handoff_rehearsal = None\n        if self.production_pilot_pre_enable_evidence is not None:\n            self.production_pilot_final_handoff_rehearsal = (\n                ProductionPilotFinalHandoffRehearsalService(\n                    pilot_control=self.production_pilot_control,\n                    pre_enable_evidence_service=(\n                        self.production_pilot_pre_enable_evidence\n                    ),\n                    preflight_resolver=self.kubernetes_preflight,\n                    production_executor_configured=(\n                        self.kubernetes_production_executor is not None\n                    ),\n                    action_runtime_production_executor_configured=(\n                        getattr(\n                            self.action_runtime,\n                            "kubernetes_production_executor",\n                            None,\n                        )\n                        is not None\n                    ),\n                )\n            )\n\n        # A dedicated database is created only when the separately gated live\n        # probe exists and the full zero-write handoff chain is available.\n        self.production_pilot_go_no_go_store = None\n        self.production_pilot_go_no_go = None\n        if (\n            self.production_pilot_live_probe is not None\n            and self.production_pilot_final_handoff_rehearsal is not None\n            and self.preflight_artifact_service is not None\n        ):\n            self.production_pilot_go_no_go_store = (\n                ProductionPilotGoNoGoStore()\n            )\n            self.production_pilot_go_no_go = (\n                ProductionPilotGoNoGoService(\n                    store=self.production_pilot_go_no_go_store,\n                    live_probe=self.production_pilot_live_probe,\n                    final_handoff_service=(\n                        self.production_pilot_final_handoff_rehearsal\n                    ),\n                    artifact_service=self.preflight_artifact_service,\n                    pilot_control=self.production_pilot_control,\n                )\n            )\n\n        self.sandbox = LocalSandboxExecutor()\n\n        self.sandbox_policy = SandboxPolicyValidator()\n\n        if self.llm_gateway is None:\n            self.llm_gateway = create_llm_gateway()\n\n        self.registry = create_agent_registry(\n            llm_gateway=self.llm_gateway,\n        )\n\n        self.planner = AgentPlanner()\n\n        self.pipeline = PlannerPipeline(\n            self.registry,\n            self.planner,\n            self.tracer,\n            self.evaluators,\n            incident_store=self.incident_store,\n            incident_service=self.incident_service,\n            workflow_service=self.workflow_service,\n        )\n\n    def create_chatops_authenticated_write_bridge(\n        self,\n        *,\n        actor_verifier: BaseChatOpsActorVerifier,\n        identity_bindings: ChatOpsIdentityBindingRegistry,\n    ) -> ChatOpsAuthenticatedWriteBridge:\n        """\n        Explicitly assemble the authenticated ChatOps write capability.\n\n        Runtime does not create this bridge automatically because a concrete\n        channel actor verifier and operator-managed identity bindings are\n        required. Merely constructing AgentRuntime therefore adds no ChatOps\n        write authority.\n\n        The bridge reuses:\n        - self.authentication for trusted OperatorIdentity,\n        - self.security_policy for RBAC,\n        - existing Approval / Action / Verification durable services.\n        """\n\n        if not isinstance(\n            actor_verifier,\n            BaseChatOpsActorVerifier,\n        ):\n            raise TypeError(\n                "ChatOps actor verifier is invalid"\n            )\n\n        if not isinstance(\n            identity_bindings,\n            ChatOpsIdentityBindingRegistry,\n        ):\n            raise TypeError(\n                "ChatOps identity bindings are invalid"\n            )\n\n        authenticator = (\n            ChatOpsIdentityAuthenticator(\n                authentication=(\n                    self.authentication\n                ),\n                bindings=(\n                    identity_bindings\n                ),\n            )\n        )\n\n        security = ChatOpsSecurityAdapter(\n            verifier=actor_verifier,\n            authenticator=authenticator,\n            policy=self.security_policy,\n        )\n\n        return ChatOpsAuthenticatedWriteBridge(\n            orchestrator=self.conversation,\n            security=security,\n            approval_service=self.approval,\n            incident_store=self.incident_store,\n            action_runtime=self.action_runtime,\n            action_execution_service=(\n                self.action_execution_service\n            ),\n            verification_service=(\n                self.verification\n            ),\n            verification_coordinator=(\n                self.verification_coordinator\n            ),\n        )\n\n    async def execute(\n        self,\n        context: AgentContext,\n    ):\n        """\n        Execute the primary PlannerPipeline and, when explicitly enabled,\n        run Investigation automatically as a best-effort Shadow.\n\n        Ordering is deliberate:\n\n        1. PlannerPipeline completes first.\n        2. Investigation receives an isolated AgentContext.\n        3. Only the bounded investigation_shadow snapshot is copied back.\n\n        Investigation can never change the Pipeline result, Incident,\n        variables, results, trace, Approval, executions or evaluations.\n\n        Investigation orchestration failure is sanitized and recorded in\n        metadata without failing an otherwise successful Pipeline execution.\n        """\n\n        if not isinstance(\n            context,\n            AgentContext,\n        ):\n            raise TypeError(\n                "AgentRuntime execution context is invalid"\n            )\n\n        # Reserved Shadow metadata from a previous execution must never be\n        # visible to the primary Pipeline, even when this Runtime currently\n        # has Investigation disabled.\n        for reserved_key in (\n            "investigation_shadow",\n            "investigation_shadow_orchestration",\n            "investigation_rca_comparison",\n            "production_multi_cluster_readiness",\n            "production_multi_cluster_live_readiness",\n            "incident_analysis_persistence",\n        ):\n            context.metadata.pop(\n                reserved_key,\n                None,\n            )\n\n        # Primary workflow semantics remain authoritative. Pipeline failure\n        # propagates normally and Investigation is not attempted afterward.\n        context.metadata.pop(\n            "incident_evidence_recorder",\n            None,\n        )\n\n        results = await self.pipeline.execute(\n            context\n        )\n\n        # Persist the authoritative Planner RCA immediately after the primary\n        # workflow. This remains weaker than the Pipeline itself: persistence\n        # failure is sanitized in metadata and cannot change Incident state.\n        await self._persist_incident_analysis(\n            context\n        )\n\n        # Evidence Recorder is evaluation-only and best-effort.\n        await self._record_incident_evidence_shadow(\n            context\n        )\n\n        if self.investigation_coordinator is None:\n            return results\n\n        shadow_context = (\n            self._create_investigation_shadow_context(\n                context\n            )\n        )\n\n        try:\n            await self.run_investigation_shadow(\n                shadow_context\n            )\n\n            readiness_snapshot = (\n                shadow_context.metadata.get(\n                    "production_multi_cluster_readiness"\n                )\n            )\n\n            if isinstance(\n                readiness_snapshot,\n                dict,\n            ):\n                context.metadata[\n                    "production_multi_cluster_readiness"\n                ] = deepcopy(\n                    readiness_snapshot\n                )\n\n            snapshot = shadow_context.metadata.get(\n                "investigation_shadow"\n            )\n\n            if (\n                not isinstance(\n                    snapshot,\n                    dict,\n                )\n                or snapshot.get(\n                    "shadow_mode"\n                )\n                is not True\n                or snapshot.get(\n                    "read_only"\n                )\n                is not True\n            ):\n                raise RuntimeError(\n                    "Investigation Shadow snapshot is invalid"\n                )\n\n            context.metadata[\n                "investigation_shadow"\n            ] = deepcopy(\n                snapshot\n            )\n\n        except Exception as exc:\n            # Shadow means Shadow: an Investigation orchestration fault must\n            # never convert a successful PlannerPipeline execution to failed.\n            #\n            # Raw exception text is deliberately excluded because provider,\n            # URL, credential or tool details may be present in it.\n            readiness_snapshot = (\n                shadow_context.metadata.get(\n                    "production_multi_cluster_readiness"\n                )\n            )\n\n            if isinstance(\n                readiness_snapshot,\n                dict,\n            ):\n                context.metadata[\n                    "production_multi_cluster_readiness"\n                ] = deepcopy(\n                    readiness_snapshot\n                )\n\n            context.metadata[\n                "investigation_shadow_orchestration"\n            ] = {\n                "shadow_mode": True,\n                "read_only": True,\n                "automatic": True,\n                "status": "failed",\n                "failure_code": (\n                    type(exc).__name__[:256]\n                ),\n            }\n\n        # Comparison is evaluation-only. It cannot change the authoritative\n        # RCA stored in context.variables["rca"] and has no Healing authority.\n        try:\n            context.metadata[\n                "investigation_rca_comparison"\n            ] = build_rca_investigation_comparison(\n                rca=context.variables.get(\n                    "rca"\n                ),\n                investigation_snapshot=(\n                    context.metadata.get(\n                        "investigation_shadow"\n                    )\n                ),\n                orchestration_snapshot=(\n                    context.metadata.get(\n                        "investigation_shadow_orchestration"\n                    )\n                ),\n            )\n        except Exception as exc:\n            # A comparison bug must remain weaker than Shadow itself and must\n            # never fail a successful primary Pipeline.\n            context.metadata[\n                "investigation_rca_comparison"\n            ] = {\n                "schema_version": "v1",\n                "shadow_mode": True,\n                "read_only": True,\n                "decision_influence": False,\n                "available": False,\n                "comparison_status": (\n                    "comparison_failed"\n                ),\n                "failure_code": (\n                    type(exc).__name__[:256]\n                ),\n            }\n\n        # Merge the bounded Investigation snapshot into the same per-Incident\n        # analysis record. Historical Memory remains independent.\n        await self._persist_incident_analysis(\n            context\n        )\n\n        return results\n\n    async def _persist_incident_analysis(\n        self,\n        context: AgentContext,\n    ) -> None:\n        """\n        Best-effort durable analysis projection for ChatOps follow-up.\n\n        This method never mutates Incident, Approval, Action or Verification.\n        A persistence fault is recorded as a bounded failure code and remains\n        weaker than the authoritative PlannerPipeline.\n        """\n\n        store = getattr(\n            self,\n            "incident_analysis_store",\n            None,\n        )\n\n        if not isinstance(\n            store,\n            IncidentAnalysisStore,\n        ):\n            return\n\n        metadata = getattr(\n            context,\n            "metadata",\n            None,\n        )\n\n        if not isinstance(\n            metadata,\n            dict,\n        ):\n            return\n\n        incident = getattr(\n            context,\n            "incident",\n            None,\n        )\n\n        incident_id = getattr(\n            incident,\n            "id",\n            None,\n        )\n\n        if incident_id is None:\n            metadata[\n                "incident_analysis_persistence"\n            ] = {\n                "schema_version": "v1",\n                "status": "skipped",\n                "reason": "incident_identity_missing",\n            }\n\n            return\n\n        try:\n            existing = await store.get(\n                incident_id\n            )\n\n            record = build_incident_analysis_record(\n                incident_id=incident_id,\n                event=context.event,\n                request_id=context.request_id,\n                primary_rca=(\n                    context.variables.get(\n                        "rca"\n                    )\n                    if isinstance(\n                        context.variables,\n                        dict,\n                    )\n                    else None\n                ),\n                investigation_snapshot=(\n                    metadata.get(\n                        "investigation_shadow"\n                    )\n                ),\n                existing=existing,\n            )\n\n            persisted = await store.upsert(\n                record\n            )\n\n            metadata[\n                "incident_analysis_persistence"\n            ] = {\n                "schema_version": "v1",\n                "status": "persisted",\n                "incident_id": str(\n                    persisted.incident_id\n                ),\n                "primary_rca": (\n                    persisted.primary_rca\n                    is not None\n                ),\n                "investigation": (\n                    persisted.investigation\n                    is not None\n                ),\n            }\n\n        except Exception as exc:\n            metadata[\n                "incident_analysis_persistence"\n            ] = {\n                "schema_version": "v1",\n                "status": "failed",\n                "failure_code": (\n                    type(exc).__name__[\n                        :256\n                    ]\n                ),\n            }\n\n    def _create_investigation_shadow_context(\n        self,\n        context: AgentContext,\n    ) -> AgentContext:\n        """\n        Build the minimum-privilege context for automatic Investigation.\n\n        Copied:\n        - event input\n        - request correlation ID\n\n        Shared:\n        - exact Runtime-owned ToolManager\n\n        Deliberately not shared:\n        - Incident\n        - variables\n        - results\n        - metadata\n        - trace\n        - memory\n        - skills\n        - MCP\n        - sandbox\n        - Approval\n        - executions\n        - evaluations\n        """\n\n        return AgentContext(\n            request_id=context.request_id,\n            event=deepcopy(\n                context.event\n            ),\n            tools=self.tools,\n            metadata={},\n        )\n\n    async def run_production_multi_cluster_live_readiness(\n        self,\n        context: AgentContext,\n        *,\n        acknowledgement: str,\n        reason: str,\n    ) -> dict[str, Any]:\n        """\n        Explicit bounded live-read production readiness proof.\n\n        This method is never called automatically by execute() or Runtime\n        startup. It records only a sanitized readiness snapshot.\n        """\n\n        if not isinstance(context, AgentContext):\n            raise TypeError(\n                "AgentRuntime live readiness requires AgentContext"\n            )\n\n        if context.tools is not self.tools:\n            raise TypeError(\n                "AgentRuntime live readiness requires shared Runtime tools"\n            )\n\n        probe = getattr(\n            self,\n            "production_multi_cluster_live_readiness",\n            None,\n        )\n\n        if probe is None:\n            raise ProductionReadinessLiveProbeError(\n                "AgentRuntime production live readiness is unavailable"\n            )\n\n        report = await probe.probe_event(\n            context.event,\n            acknowledgement=acknowledgement,\n            reason=reason,\n        )\n\n        snapshot = report.snapshot()\n\n        context.metadata[\n            "production_multi_cluster_live_readiness"\n        ] = deepcopy(snapshot)\n\n        return snapshot\n\n    async def run_investigation_shadow(\n        self,\n        context: AgentContext,\n    ) -> InvestigationState:\n        """\n        Explicitly execute the enabled read-only Investigation Shadow.\n\n        This method is intentionally separate from PlannerPipeline.\n\n        PlannerPipeline itself never invokes Investigation. AgentRuntime\n        may call this lower-level entry point after a successful Pipeline\n        execution when automatic Shadow Investigation is enabled.\n\n        The supplied AgentContext must use the exact Runtime ToolManager so\n        Investigation probes cannot bypass Runtime-owned tool boundaries.\n        """\n\n        if not isinstance(\n            context,\n            AgentContext,\n        ):\n            raise TypeError(\n                "AgentRuntime Investigation Shadow context is invalid"\n            )\n\n        if self.investigation_coordinator is None:\n            raise RuntimeError(\n                "AgentRuntime Investigation Shadow is disabled"\n            )\n\n        if context.tools is not self.tools:\n            raise TypeError(\n                "AgentRuntime Investigation Shadow requires shared Runtime tools"\n            )\n\n        if getattr(\n            self,\n            "cluster_verified_evidence_required",\n            False,\n        ):\n            if (\n                self.production_multi_cluster_readiness\n                is None\n            ):\n                raise ProductionMultiClusterReadinessError(\n                    "AgentRuntime Production Shadow readiness proof is unavailable"\n                )\n\n            readiness = (\n                self.production_multi_cluster_readiness\n                .evaluate_event(\n                    context.event\n                )\n            )\n\n            context.metadata[\n                "production_multi_cluster_readiness"\n            ] = readiness.snapshot()\n\n            if not readiness.ready:\n                raise ProductionMultiClusterReadinessError(\n                    "AgentRuntime Production Shadow read coverage is not ready"\n                )\n\n        return await (\n            self.investigation_coordinator.investigate(\n                context\n            )\n        )\n\n    async def _record_incident_evidence_shadow(\n        self,\n        context: AgentContext,\n    ) -> None:\n        """\n        Best-effort, decision-isolated production evidence preservation.\n\n        Runs only after the authoritative PlannerPipeline succeeds.\n        Disabled mode constructs no Recorder and issues no production Probe.\n        """\n\n        try:\n            settings = (\n                IncidentEvidenceRecorderSettings\n                .from_environment()\n            )\n        except Exception as exc:\n            context.metadata[\n                "incident_evidence_recorder"\n            ] = {\n                "schema_version": "v1",\n                "shadow_mode": True,\n                "read_only": True,\n                "decision_influence": False,\n                "automatic": True,\n                "status": "failed",\n                "failure_code": (\n                    type(exc).__name__[:256]\n                ),\n            }\n            return\n\n        if not settings.enabled:\n            return\n\n        recorder_context = AgentContext(\n            request_id=context.request_id,\n            event=deepcopy(\n                context.event\n            ),\n            tools=self.tools,\n            metadata={},\n        )\n\n        try:\n            recorder = ProductionIncidentEvidenceRecorder(\n                settings.resolve_output_dir()\n            )\n\n            result = await recorder.record(\n                recorder_context\n            )\n\n            context.metadata[\n                "incident_evidence_recorder"\n            ] = {\n                "schema_version": "v1",\n                "shadow_mode": True,\n                "read_only": True,\n                "decision_influence": False,\n                "automatic": True,\n                "status": "captured",\n                "created": result.created,\n                "incident_id": result.incident_id,\n                "observation_count": (\n                    result.observation_count\n                ),\n                "capture_file": result.path.name,\n            }\n\n        except Exception as exc:\n            context.metadata[\n                "incident_evidence_recorder"\n            ] = {\n                "schema_version": "v1",\n                "shadow_mode": True,\n                "read_only": True,\n                "decision_influence": False,\n                "automatic": True,\n                "status": "failed",\n                "failure_code": (\n                    type(exc).__name__[:256]\n                ),\n            }\n', 'services/agent_runtime/tests/test_conversation_orchestrator.py': 'from __future__ import annotations\n\nimport pytest\n\nfrom services.agent_runtime.app.conversation import (\n    ConversationEvidenceView,\n    ConversationHypothesisView,\n    ConversationIncidentContext,\n    ConversationIntent,\n    ConversationOrchestrator,\n    ConversationReplyMode,\n    ConversationTurnRequest,\n    DeterministicConversationIntentClassifier,\n    DictConversationIncidentContextProvider,\n)\n\n\ndef context(incident_id="INC-1001"):\n    return ConversationIncidentContext(\n        incident_id=incident_id,\n        status="waiting_approval",\n        title="payment-api PodOOMKilled",\n        root_cause=(\n            "Memory limit regression caused OOMKilled"\n        ),\n        root_cause_confidence=0.94,\n        evidence=(\n            ConversationEvidenceView(\n                evidence_id="ev-1",\n                source="kubernetes",\n                summary="Container terminated with OOMKilled",\n                trusted=True,\n                cluster_verified=True,\n            ),\n            ConversationEvidenceView(\n                evidence_id="ev-2",\n                source="prometheus",\n                summary="Memory utilization reached 99%",\n                trusted=True,\n                cluster_verified=True,\n            ),\n        ),\n        hypotheses=(\n            ConversationHypothesisView(\n                cause="Memory limit regression",\n                confidence=0.94,\n            ),\n            ConversationHypothesisView(\n                cause="Memory leak",\n                confidence=0.21,\n            ),\n        ),\n        recommended_action=(\n            "Restore memory limit from 512Mi to 1Gi"\n        ),\n        action_risk="medium",\n        approval_status="pending",\n    )\n\n\ndef orchestrator():\n    provider = DictConversationIncidentContextProvider(\n        {\n            "INC-1001": context(),\n            "INC-2002": context("INC-2002"),\n        }\n    )\n    return ConversationOrchestrator(\n        provider=provider\n    )\n\n\n@pytest.mark.parametrize(\n    ("text", "intent"),\n    [\n        ("现在状态怎么样？", ConversationIntent.STATUS),\n        ("根因是什么？", ConversationIntent.RCA),\n        ("为什么这么判断？", ConversationIntent.EVIDENCE),\n        ("有哪些证据？", ConversationIntent.EVIDENCE),\n        ("下一步怎么办？", ConversationIntent.NEXT_STEP),\n        ("验证结果怎么样？", ConversationIntent.VERIFICATION),\n        ("帮我修一下", ConversationIntent.REMEDIATE),\n        ("批准执行", ConversationIntent.APPROVE),\n        ("拒绝", ConversationIntent.REJECT),\n        ("help", ConversationIntent.HELP),\n    ],\n)\ndef test_deterministic_intent_classifier(\n    text,\n    intent,\n):\n    classifier = (\n        DeterministicConversationIntentClassifier()\n    )\n    assert classifier.classify(text) == intent\n\n\n@pytest.mark.asyncio\nasync def test_first_turn_requires_incident_binding():\n    value = orchestrator()\n    reply = await value.handle(\n        ConversationTurnRequest(\n            conversation_id="chat-1",\n            text="根因是什么？",\n        )\n    )\n    assert reply.mode == (\n        ConversationReplyMode.NEEDS_INCIDENT\n    )\n    assert reply.incident_id is None\n\n\n@pytest.mark.asyncio\nasync def test_explicit_incident_binding_is_reused_by_follow_up_turns():\n    value = orchestrator()\n\n    first = await value.handle(\n        ConversationTurnRequest(\n            conversation_id="chat-1",\n            incident_id="INC-1001",\n            text="现在状态怎么样？",\n        )\n    )\n\n    second = await value.handle(\n        ConversationTurnRequest(\n            conversation_id="chat-1",\n            text="根因是什么？",\n        )\n    )\n\n    assert first.incident_id == "INC-1001"\n    assert second.incident_id == "INC-1001"\n\n    session = await value.sessions.get("chat-1")\n    assert session is not None\n    assert session.incident_id == "INC-1001"\n    assert session.turn_count == 2\n\n\n@pytest.mark.asyncio\nasync def test_explicit_new_incident_rebinds_conversation():\n    value = orchestrator()\n\n    await value.handle(\n        ConversationTurnRequest(\n            conversation_id="chat-1",\n            incident_id="INC-1001",\n            text="状态",\n        )\n    )\n\n    reply = await value.handle(\n        ConversationTurnRequest(\n            conversation_id="chat-1",\n            incident_id="INC-2002",\n            text="状态",\n        )\n    )\n    assert reply.incident_id == "INC-2002"\n\n\n@pytest.mark.asyncio\nasync def test_rca_reply_uses_existing_context_without_llm():\n    value = orchestrator()\n\n    reply = await value.handle(\n        ConversationTurnRequest(\n            conversation_id="chat-2",\n            incident_id="INC-1001",\n            text="根因是什么？",\n        )\n    )\n\n    assert reply.intent == ConversationIntent.RCA\n    assert reply.mode == ConversationReplyMode.READ_ONLY\n\n    text = str(reply.model_dump())\n    assert (\n        "Memory limit regression caused OOMKilled"\n        in text\n    )\n    assert "94%" in text\n\n\n@pytest.mark.asyncio\nasync def test_evidence_reply_preserves_sources():\n    value = orchestrator()\n\n    reply = await value.handle(\n        ConversationTurnRequest(\n            conversation_id="chat-3",\n            incident_id="INC-1001",\n            text="证据是什么？",\n        )\n    )\n\n    text = str(reply.model_dump())\n    assert "OOMKilled" in text\n    assert "99%" in text\n    assert "kubernetes" in text\n    assert "prometheus" in text\n\n\n@pytest.mark.asyncio\nasync def test_write_intents_return_explicit_nonexecuting_boundary():\n    value = orchestrator()\n\n    for text, expected_operation in (\n        ("批准执行", "approval.approve"),\n        ("拒绝", "approval.reject"),\n        ("帮我修一下", "action.resume"),\n    ):\n        reply = await value.handle(\n            ConversationTurnRequest(\n                conversation_id=(\n                    "chat-write-"\n                    + expected_operation\n                ),\n                incident_id="INC-1001",\n                text=text,\n            )\n        )\n\n        assert reply.mode == (\n            ConversationReplyMode\n            .WRITE_ACTION_REQUIRED\n        )\n        assert (\n            reply.write_operation\n            == expected_operation\n        )\n\n\n@pytest.mark.asyncio\nasync def test_unknown_incident_is_explicit():\n    value = orchestrator()\n\n    reply = await value.handle(\n        ConversationTurnRequest(\n            conversation_id="chat-4",\n            incident_id="INC-404",\n            text="状态",\n        )\n    )\n\n    assert reply.mode == (\n        ConversationReplyMode\n        .INCIDENT_NOT_FOUND\n    )\n\n\ndef test_conversation_core_has_no_runtime_write_authority():\n    from pathlib import Path\n    import services.agent_runtime.app.conversation.orchestrator as module\n\n    source = Path(module.__file__).read_text(\n        encoding="utf-8"\n    )\n\n    forbidden = [\n        "ActionRuntime",\n        "ApprovalService",\n        "KubernetesProductionExecutor",\n        ".approve(",\n        ".reject(",\n        ".resume(",\n        ".execute(",\n    ]\n\n    assert [\n        item\n        for item in forbidden\n        if item in source\n    ] == []\n', 'services/agent_runtime/tests/test_chatops_authenticated_write_bridge.py': 'from __future__ import annotations\n\nfrom types import SimpleNamespace\n\nimport pytest\n\nfrom services.agent_runtime.app.action.models import (\n    ActionPlan,\n    ActionType,\n)\nfrom services.agent_runtime.app.approval.models import (\n    ApprovalStatus,\n)\nfrom services.agent_runtime.app.conversation import (\n    BaseChatOpsActorVerifier,\n    ChatOpsActorVerificationError,\n    ChatOpsConversationRef,\n    ChatOpsIdentityBinding,\n    ChatOpsIdentityBindingRegistry,\n    ChatOpsInboundMessage,\n    ChatOpsVerifiedActor,\n    ChatOpsWriteStatus,\n)\nfrom services.agent_runtime.app.incident.enums import (\n    IncidentStatus,\n)\nfrom services.agent_runtime.app.incident.state import (\n    IncidentState,\n)\nfrom services.agent_runtime.app.runtime.runtime import (\n    AgentRuntime,\n)\nfrom services.agent_runtime.app.security.authentication import (\n    ApiKeyAuthenticationProvider,\n    ApiKeyRecord,\n)\nfrom services.agent_runtime.app.security.models import (\n    OperatorRole,\n)\nfrom services.agent_runtime.app.security.service import (\n    AuthenticationProviderRegistry,\n    AuthenticationService,\n)\n\n\nAPPROVER_SECRET = (\n    "chatops-approver-secret-000000000000001"\n)\nEXECUTOR_SECRET = (\n    "chatops-executor-secret-000000000000001"\n)\n\n\nclass ExactActorVerifier(\n    BaseChatOpsActorVerifier\n):\n    def __init__(\n        self,\n        allowed,\n    ):\n        self.allowed = set(\n            allowed\n        )\n        self.calls = []\n\n    async def verify(\n        self,\n        message,\n    ):\n        self.calls.append(\n            message.message_id\n        )\n\n        actor_id = (\n            message.external_actor_id\n        )\n\n        if actor_id not in self.allowed:\n            raise ChatOpsActorVerificationError(\n                "test actor is not verified"\n            )\n\n        return ChatOpsVerifiedActor(\n            channel=(\n                message.conversation.channel\n            ),\n            tenant_id=(\n                message.conversation.tenant_id\n            ),\n            external_actor_id=actor_id,\n            verification_method=(\n                "unit_test_verified_transport"\n            ),\n        )\n\n\nclass MismatchedActorVerifier(\n    BaseChatOpsActorVerifier\n):\n    async def verify(\n        self,\n        message,\n    ):\n        return ChatOpsVerifiedActor(\n            channel=(\n                message.conversation.channel\n            ),\n            tenant_id=(\n                message.conversation.tenant_id\n            ),\n            external_actor_id=(\n                "different-verified-actor"\n            ),\n            verification_method=(\n                "unit_test_verified_transport"\n            ),\n        )\n\n\ndef authentication_service():\n    provider = ApiKeyAuthenticationProvider(\n        [\n            ApiKeyRecord.from_plaintext(\n                key_id="approver-key",\n                api_key=APPROVER_SECRET,\n                principal_id="approver-1",\n                roles={\n                    OperatorRole.APPROVER,\n                },\n            ),\n            ApiKeyRecord.from_plaintext(\n                key_id="executor-key",\n                api_key=EXECUTOR_SECRET,\n                principal_id="executor-1",\n                roles={\n                    OperatorRole.EXECUTOR,\n                },\n            ),\n        ]\n    )\n\n    return AuthenticationService(\n        AuthenticationProviderRegistry(\n            [\n                provider\n            ]\n        )\n    )\n\n\ndef bindings():\n    return ChatOpsIdentityBindingRegistry(\n        [\n            ChatOpsIdentityBinding(\n                channel="feishu",\n                tenant_id="tenant-a",\n                external_actor_id=(\n                    "actor-approver"\n                ),\n                expected_principal_id=(\n                    "approver-1"\n                ),\n                credential_env=(\n                    "CHATOPS_APPROVER_API_KEY"\n                ),\n            ),\n            ChatOpsIdentityBinding(\n                channel="feishu",\n                tenant_id="tenant-a",\n                external_actor_id=(\n                    "actor-executor"\n                ),\n                expected_principal_id=(\n                    "executor-1"\n                ),\n                credential_env=(\n                    "CHATOPS_EXECUTOR_API_KEY"\n                ),\n            ),\n        ]\n    )\n\n\ndef conversation():\n    return ChatOpsConversationRef(\n        channel="feishu",\n        tenant_id="tenant-a",\n        conversation_id="sre-group",\n        thread_id="incident-thread",\n    )\n\n\ndef inbound(\n    *,\n    actor,\n    text,\n    incident_id,\n    message_id,\n):\n    return ChatOpsInboundMessage(\n        conversation=conversation(),\n        message_id=message_id,\n        external_actor_id=actor,\n        text=text,\n        incident_id=incident_id,\n    )\n\n\ndef create_runtime(\n    monkeypatch,\n    tmp_path,\n):\n    monkeypatch.chdir(\n        tmp_path\n    )\n\n    monkeypatch.setenv(\n        "CHATOPS_APPROVER_API_KEY",\n        APPROVER_SECRET,\n    )\n\n    monkeypatch.setenv(\n        "CHATOPS_EXECUTOR_API_KEY",\n        EXECUTOR_SECRET,\n    )\n\n    for name in (\n        "PROMETHEUS_URL",\n        "KUBERNETES_API_URL",\n        "KUBERNETES_SERVICE_HOST",\n        "KUBERNETES_SERVICE_PORT",\n        "KUBERNETES_SERVICE_PORT_HTTPS",\n    ):\n        monkeypatch.delenv(\n            name,\n            raising=False,\n        )\n\n    monkeypatch.setenv(\n        "PROMETHEUS_ALLOW_MOCK_FALLBACK",\n        "true",\n    )\n\n    monkeypatch.setenv(\n        "KUBERNETES_ALLOW_DRY_RUN_FALLBACK",\n        "true",\n    )\n\n    return AgentRuntime(\n        authentication_service=(\n            authentication_service()\n        )\n    )\n\n\nasync def create_incident_and_approval(\n    runtime,\n):\n    incident = IncidentState(\n        status=IncidentStatus.CONFIRMED,\n        reason="RCA complete",\n    )\n\n    incident = await (\n        runtime.incident_store.save(\n            incident\n        )\n    )\n\n    plan = ActionPlan(\n        type=(\n            ActionType.INCREASE_MEMORY_LIMIT\n        ),\n        target="checkout-api",\n        namespace="checkout",\n        cluster="prod-us-03",\n    )\n\n    approval = await (\n        runtime.approval.create_approval(\n            action=plan,\n            reason="AI recommends remediation",\n            incident_id=incident.id,\n        )\n    )\n\n    return (\n        incident,\n        approval,\n    )\n\n\n@pytest.mark.asyncio\nasync def test_verified_approver_can_approve_and_exact_webhook_replay_is_idempotent(\n    monkeypatch,\n    tmp_path,\n):\n    runtime = create_runtime(\n        monkeypatch,\n        tmp_path,\n    )\n\n    (\n        incident,\n        approval,\n    ) = await create_incident_and_approval(\n        runtime\n    )\n\n    verifier = ExactActorVerifier(\n        {\n            "actor-approver",\n            "actor-executor",\n        }\n    )\n\n    bridge = (\n        runtime\n        .create_chatops_authenticated_write_bridge(\n            actor_verifier=verifier,\n            identity_bindings=bindings(),\n        )\n    )\n\n    message = inbound(\n        actor="actor-approver",\n        text="批准执行",\n        incident_id=str(\n            incident.id\n        ),\n        message_id="msg-approve-1",\n    )\n\n    first = await bridge.handle(\n        message\n    )\n\n    assert first.success is True\n    assert (\n        first.status\n        == ChatOpsWriteStatus.APPROVED\n    )\n    assert first.approval_id == (\n        approval.id\n    )\n    assert first.operator_id == (\n        "approver-1"\n    )\n    assert (\n        first.idempotent_replay\n        is False\n    )\n\n    stored = await runtime.approval.get(\n        approval.id\n    )\n\n    assert stored is not None\n    assert stored.status == (\n        ApprovalStatus.APPROVED\n    )\n    assert stored.decision is not None\n    assert stored.decision.operator_id == (\n        "approver-1"\n    )\n    assert stored.decision.metadata[\n        "source"\n    ] == "chatops"\n    assert stored.decision.metadata[\n        "protected_operation"\n    ] == "approval.decide"\n\n    serialized = str(\n        stored.decision.model_dump(\n            mode="json"\n        )\n    )\n\n    assert "actor-approver" not in (\n        serialized\n    )\n    assert APPROVER_SECRET not in (\n        serialized\n    )\n\n    second = await bridge.handle(\n        message\n    )\n\n    assert second.success is True\n    assert (\n        second.status\n        == ChatOpsWriteStatus.APPROVED\n    )\n    assert (\n        second.idempotent_replay\n        is True\n    )\n\n\n@pytest.mark.asyncio\nasync def test_spoofed_external_actor_fails_before_approval_domain_read(\n    monkeypatch,\n    tmp_path,\n):\n    runtime = create_runtime(\n        monkeypatch,\n        tmp_path,\n    )\n\n    incident, _ = (\n        await create_incident_and_approval(\n            runtime\n        )\n    )\n\n    bridge = (\n        runtime\n        .create_chatops_authenticated_write_bridge(\n            actor_verifier=(\n                MismatchedActorVerifier()\n            ),\n            identity_bindings=bindings(),\n        )\n    )\n\n    calls = []\n\n    async def forbidden(\n        incident_id,\n    ):\n        calls.append(\n            incident_id\n        )\n        raise AssertionError(\n            "Actor spoofing reached Approval domain read"\n        )\n\n    monkeypatch.setattr(\n        runtime.approval,\n        "list_by_incident",\n        forbidden,\n    )\n\n    outcome = await bridge.handle(\n        inbound(\n            actor="actor-approver",\n            text="批准执行",\n            incident_id=str(\n                incident.id\n            ),\n            message_id="msg-spoof-1",\n        )\n    )\n\n    assert outcome.success is False\n    assert outcome.status == (\n        ChatOpsWriteStatus\n        .ACTOR_VERIFICATION_FAILED\n    )\n    assert outcome.incident_id is None\n    assert calls == []\n\n\n@pytest.mark.asyncio\nasync def test_approver_cannot_execute_and_denial_happens_before_domain_read(\n    monkeypatch,\n    tmp_path,\n):\n    runtime = create_runtime(\n        monkeypatch,\n        tmp_path,\n    )\n\n    (\n        incident,\n        approval,\n    ) = await create_incident_and_approval(\n        runtime\n    )\n\n    await runtime.approval.approve(\n        approval.id,\n        operator_id="approver-1",\n        idempotency_key=(\n            "preapproved-test-key"\n        ),\n        reason="approved before executor test",\n        metadata={\n            "source": "test",\n        },\n    )\n\n    bridge = (\n        runtime\n        .create_chatops_authenticated_write_bridge(\n            actor_verifier=(\n                ExactActorVerifier(\n                    {\n                        "actor-approver",\n                        "actor-executor",\n                    }\n                )\n            ),\n            identity_bindings=bindings(),\n        )\n    )\n\n    calls = []\n\n    async def forbidden(\n        incident_id,\n    ):\n        calls.append(\n            incident_id\n        )\n        raise AssertionError(\n            "Unauthorized approver reached Action domain read"\n        )\n\n    monkeypatch.setattr(\n        runtime.approval,\n        "list_by_incident",\n        forbidden,\n    )\n\n    outcome = await bridge.handle(\n        inbound(\n            actor="actor-approver",\n            text="执行修复",\n            incident_id=str(\n                incident.id\n            ),\n            message_id="msg-execute-denied",\n        )\n    )\n\n    assert outcome.success is False\n    assert outcome.status == (\n        ChatOpsWriteStatus\n        .AUTHORIZATION_DENIED\n    )\n    assert outcome.incident_id is None\n    assert calls == []\n\n\n@pytest.mark.asyncio\nasync def test_executor_path_uses_authenticated_principal_and_runs_verification(\n    monkeypatch,\n    tmp_path,\n):\n    runtime = create_runtime(\n        monkeypatch,\n        tmp_path,\n    )\n\n    (\n        incident,\n        approval,\n    ) = await create_incident_and_approval(\n        runtime\n    )\n\n    approved = await (\n        runtime.approval.approve(\n            approval.id,\n            operator_id="approver-1",\n            idempotency_key=(\n                "approval-before-execution"\n            ),\n            reason="approved for executor",\n            metadata={\n                "source": "test",\n            },\n        )\n    )\n\n    assert approved.status == (\n        ApprovalStatus.APPROVED\n    )\n\n    bridge = (\n        runtime\n        .create_chatops_authenticated_write_bridge(\n            actor_verifier=(\n                ExactActorVerifier(\n                    {\n                        "actor-approver",\n                        "actor-executor",\n                    }\n                )\n            ),\n            identity_bindings=bindings(),\n        )\n    )\n\n    get_calls = {\n        "count": 0,\n    }\n\n    fake_execution = SimpleNamespace(\n        id="execution-1",\n        status=SimpleNamespace(\n            value="succeeded"\n        ),\n        operator_id="executor-1",\n        idempotency_key="placeholder",\n    )\n\n    async def fake_get_by_approval(\n        approval_id,\n    ):\n        assert approval_id == (\n            approval.id\n        )\n\n        get_calls[\n            "count"\n        ] += 1\n\n        if get_calls[\n            "count"\n        ] == 1:\n            return None\n\n        return fake_execution\n\n    monkeypatch.setattr(\n        runtime.action_execution_service,\n        "get_by_approval",\n        fake_get_by_approval,\n    )\n\n    resume_calls = []\n\n    async def fake_resume(\n        approval_id,\n        incident=None,\n        *,\n        operator_id=None,\n        idempotency_key=None,\n    ):\n        resume_calls.append(\n            {\n                "approval_id": approval_id,\n                "incident_id": str(\n                    incident.id\n                ),\n                "operator_id": operator_id,\n                "idempotency_key": (\n                    idempotency_key\n                ),\n            }\n        )\n\n        fake_execution.idempotency_key = (\n            idempotency_key\n        )\n\n        return {\n            "success": True,\n            "status": "succeeded",\n            "execution_id": (\n                "execution-1"\n            ),\n            "idempotent_replay": False,\n        }\n\n    monkeypatch.setattr(\n        runtime.action_runtime,\n        "resume",\n        fake_resume,\n    )\n\n    async def no_existing_verification(\n        action_execution_id,\n    ):\n        assert str(\n            action_execution_id\n        ) == "execution-1"\n\n        return None\n\n    monkeypatch.setattr(\n        runtime.verification,\n        "get_by_action_execution",\n        no_existing_verification,\n    )\n\n    verification_calls = []\n\n    async def fake_verification_run(\n        **kwargs,\n    ):\n        verification_calls.append(\n            dict(\n                kwargs\n            )\n        )\n\n        return (\n            SimpleNamespace(\n                id="verification-1",\n                status=SimpleNamespace(\n                    value="passed"\n                ),\n            ),\n            incident,\n        )\n\n    monkeypatch.setattr(\n        runtime.verification_coordinator,\n        "run",\n        fake_verification_run,\n    )\n\n    outcome = await bridge.handle(\n        inbound(\n            actor="actor-executor",\n            text="执行修复",\n            incident_id=str(\n                incident.id\n            ),\n            message_id="msg-execute-1",\n        )\n    )\n\n    assert outcome.success is True\n    assert outcome.status == (\n        ChatOpsWriteStatus\n        .EXECUTION_COMPLETED\n    )\n    assert outcome.operator_id == (\n        "executor-1"\n    )\n    assert outcome.execution_status == (\n        "succeeded"\n    )\n    assert outcome.verification_status == (\n        "passed"\n    )\n\n    assert len(\n        resume_calls\n    ) == 1\n    assert resume_calls[\n        0\n    ][\n        "operator_id"\n    ] == "executor-1"\n\n    assert resume_calls[\n        0\n    ][\n        "idempotency_key"\n    ].startswith(\n        "chatops:"\n    )\n\n    assert len(\n        verification_calls\n    ) == 1\n    assert verification_calls[\n        0\n    ][\n        "metadata"\n    ][\n        "source"\n    ] == "chatops"\n\n\n@pytest.mark.asyncio\nasync def test_missing_actor_binding_fails_before_domain_work(\n    monkeypatch,\n    tmp_path,\n):\n    runtime = create_runtime(\n        monkeypatch,\n        tmp_path,\n    )\n\n    incident, _ = (\n        await create_incident_and_approval(\n            runtime\n        )\n    )\n\n    verifier = ExactActorVerifier(\n        {\n            "unbound-actor",\n        }\n    )\n\n    bridge = (\n        runtime\n        .create_chatops_authenticated_write_bridge(\n            actor_verifier=verifier,\n            identity_bindings=bindings(),\n        )\n    )\n\n    calls = []\n\n    async def forbidden(\n        incident_id,\n    ):\n        calls.append(\n            incident_id\n        )\n        raise AssertionError(\n            "Unbound actor reached Approval domain"\n        )\n\n    monkeypatch.setattr(\n        runtime.approval,\n        "list_by_incident",\n        forbidden,\n    )\n\n    outcome = await bridge.handle(\n        inbound(\n            actor="unbound-actor",\n            text="批准执行",\n            incident_id=str(\n                incident.id\n            ),\n            message_id="msg-unbound-1",\n        )\n    )\n\n    assert outcome.success is False\n    assert outcome.status == (\n        ChatOpsWriteStatus\n        .AUTHENTICATION_FAILED\n    )\n    assert calls == []\n\n\ndef test_chatops_identity_and_write_bridge_do_not_define_roles_or_credentials():\n    from pathlib import Path\n\n    import services.agent_runtime.app.conversation.identity as identity_module\n    import services.agent_runtime.app.conversation.write_bridge as write_module\n\n    identity_source = Path(\n        identity_module.__file__\n    ).read_text(\n        encoding="utf-8"\n    )\n\n    write_source = Path(\n        write_module.__file__\n    ).read_text(\n        encoding="utf-8"\n    )\n\n    assert "DEFAULT_ROLE_PERMISSIONS" not in (\n        identity_source\n        + write_source\n    )\n\n    assert "ApiKeyRecord" not in (\n        identity_source\n        + write_source\n    )\n\n    assert "external_actor_id" not in (\n        write_source\n    )\n\n    forbidden_write_shortcuts = [\n        "KubernetesProductionExecutor",\n        "httpx",\n        "requests.",\n        "aiohttp",\n    ]\n\n    assert [\n        item\n        for item in forbidden_write_shortcuts\n        if item in write_source\n    ] == []\n'}


@dataclass(frozen=True)
class CommandResult:
    name: str
    command: list[str]
    returncode: int
    stdout: str
    stderr: str


def find_repo_root(
    start: Path,
) -> Path:
    for candidate in (
        start,
        *start.parents,
    ):
        if (
            (candidate / "pyproject.toml").exists()
            and (candidate / "services").exists()
            and (candidate / "packages").exists()
        ):
            return candidate

    raise RuntimeError(
        "Repository root not found."
    )


def write_text(
    path: Path,
    value: str,
) -> None:
    path.parent.mkdir(
        parents=True,
        exist_ok=True,
    )

    normalized = (
        value
        .replace("\r\n", "\n")
        .replace("\r", "\n")
    )

    if not normalized.endswith(
        "\n"
    ):
        normalized += "\n"

    path.write_text(
        normalized,
        encoding="utf-8",
        newline="\n",
    )


def raw_sha256(
    path: Path,
) -> str:
    return hashlib.sha256(
        path.read_bytes()
    ).hexdigest()


def backup_file(
    path: Path,
) -> Path:
    stamp = datetime.now().strftime(
        "%Y%m%d_%H%M%S"
    )

    backup = path.with_name(
        f"{path.name}.before_{VERSION}_{stamp}.bak"
    )

    shutil.copy2(
        path,
        backup,
    )

    return backup


def run_command(
    *,
    root: Path,
    name: str,
    command: list[str],
) -> CommandResult:
    process = subprocess.run(
        command,
        cwd=root,
        capture_output=True,
        text=True,
        encoding="utf-8",
        errors="replace",
        check=False,
    )

    return CommandResult(
        name=name,
        command=command,
        returncode=process.returncode,
        stdout=process.stdout,
        stderr=process.stderr,
    )


def section(
    lines: list[str],
    title: str,
) -> None:
    lines.extend(
        [
            "",
            "=" * 120,
            title,
            "=" * 120,
            "",
        ]
    )


def add_command(
    lines: list[str],
    result: CommandResult,
) -> None:
    section(
        lines,
        f"COMMAND: {result.name}",
    )

    lines.extend(
        [
            " ".join(
                result.command
            ),
            "",
            f"ExitCode: {result.returncode}",
            "",
            "STDOUT",
            "-" * 120,
            result.stdout.rstrip()
            or "<EMPTY>",
            "",
            "STDERR",
            "-" * 120,
            result.stderr.rstrip()
            or "<EMPTY>",
        ]
    )


def verify_hash(
    *,
    root: Path,
    relative: str,
) -> None:
    path = root / relative

    if not path.exists():
        raise RuntimeError(
            f"Required reviewed file is missing: {relative}"
        )

    actual = raw_sha256(
        path
    )

    expected = EXPECTED_RAW_HASHES[
        relative
    ]

    if actual != expected:
        raise RuntimeError(
            (
                f"{relative} changed after the reviewed ChatOps security snapshot. "
                f"expected_raw_sha256={expected} actual_raw_sha256={actual}. "
                "Refusing stale ChatOps Identity / Authenticated Write Bridge installation."
            )
        )


def require_tests(
    root: Path,
    values: list[str],
) -> list[str]:
    missing = [
        value
        for value in values
        if not (
            root
            / value
        ).exists()
    ]

    if missing:
        raise RuntimeError(
            "Required compatibility tests are missing: "
            + ", ".join(
                missing
            )
        )

    return values


def main() -> int:
    root = find_repo_root(
        Path.cwd().resolve()
    )

    after = root / AFTER_NAME
    error = root / ERROR_NAME

    for output in (
        after,
        error,
    ):
        try:
            output.unlink()
        except FileNotFoundError:
            pass

    targets = {
        (
            root
            / relative
        ): source
        for relative, source
        in SOURCES.items()
    }

    preexisting = {
        path: path.exists()
        for path in targets
    }

    backups = []

    report = [
        "ChatOps Identity + Authenticated Write Bridge v1",
        (
            "GeneratedAt: "
            + datetime.now().astimezone().isoformat()
        ),
        "",
        "Product direction:",
        "- ChatOps-first, Evidence-driven, Human-in-the-loop AI SRE Agent",
        "- reuse existing Runtime AuthenticationService and SecurityPolicyEngine",
        "- no second ChatOps role/permission system",
        "",
        "Identity boundary:",
        "- raw external_actor_id never grants Runtime identity or role",
        "- BaseChatOpsActorVerifier must produce an exact verified actor",
        "- verified actor resolves through operator-configured ChatOpsIdentityBindingRegistry",
        "- binding contains only an environment credential reference + expected principal",
        "- plaintext credential is read only for AuthenticationService.authenticate() and is not retained",
        "- authenticated principal must exactly match expected_principal_id",
        "",
        "RBAC boundary:",
        "- approve/reject -> ProtectedOperation.DECIDE_APPROVAL",
        "- execute remediation -> ProtectedOperation.RESUME_ACTION",
        "- SecurityPolicyEngine is evaluated before Approval/Incident/Action/Verification domain reads",
        "- Approver cannot execute and Executor cannot approve unless Admin role already grants both",
        "",
        "ChatOps semantics:",
        "- APPROVE -> approval.approve",
        "- REJECT -> approval.reject",
        "- REMEDIATE / execute remediation -> action.resume",
        "- Action resume uses the existing ActionRuntime durable execution boundary",
        "- successful execution triggers the existing VerificationCoordinator exactly-once path",
        "",
        "Idempotency:",
        "- approval and execution keys are derived from opaque conversation binding + message id/text + operation + target",
        "- exact webhook replay returns durable state instead of a second mutation",
        "- edited message text produces a different idempotency key",
        "",
        "Runtime:",
        "- does NOT create an authenticated write bridge automatically",
        "- create_chatops_authenticated_write_bridge(...) requires an explicit actor verifier and identity binding registry",
        "- default runtime.chatops remains the non-writing conversation gateway",
        "",
        "Installer tests use local Runtime state and mocks only.",
        "No real channel network/Kubernetes/Prometheus/LLM request is sent.",
    ]

    try:
        section(
            report,
            "CURRENT RAW HASH PREFLIGHT",
        )

        for relative in EXPECTED_RAW_HASHES:
            verify_hash(
                root=root,
                relative=relative,
            )

            report.append(
                relative
                + "="
                + EXPECTED_RAW_HASHES[
                    relative
                ]
            )

        for path in targets:
            relative = str(
                path.relative_to(
                    root
                )
            ).replace(
                "\\",
                "/",
            )

            if (
                relative
                not in EXPECTED_RAW_HASHES
                and path.exists()
            ):
                raise RuntimeError(
                    "New ChatOps Identity / Write target already exists; refusing overwrite: "
                    + relative
                )

        section(
            report,
            "BACKUP",
        )

        for path in targets:
            if path.exists():
                backup = backup_file(
                    path
                )

                backups.append(
                    (
                        path,
                        backup,
                    )
                )

                report.append(
                    "backup="
                    + str(
                        backup.relative_to(
                            root
                        )
                    )
                )

        for path, source in targets.items():
            write_text(
                path,
                source,
            )

        syntax = run_command(
            root=root,
            name="Python syntax",
            command=[
                "uv",
                "run",
                "python",
                "-m",
                "py_compile",
                *[
                    str(
                        path.relative_to(
                            root
                        )
                    )
                    for path in targets
                ],
            ],
        )

        add_command(
            report,
            syntax,
        )

        if syntax.returncode != 0:
            raise RuntimeError(
                "ChatOps Identity / Write Bridge syntax failed"
            )

        focused = run_command(
            root=root,
            name="ChatOps authenticated write focused tests",
            command=[
                "uv",
                "run",
                "pytest",
                (
                    "services/agent_runtime/tests/"
                    "test_chatops_authenticated_write_bridge.py"
                ),
                (
                    "services/agent_runtime/tests/"
                    "test_conversation_orchestrator.py"
                ),
                (
                    "services/agent_runtime/tests/"
                    "test_durable_conversation_chatops_contract.py"
                ),
                (
                    "services/agent_runtime/tests/"
                    "test_incident_analysis_conversation_context.py"
                ),
                "-q",
            ],
        )

        add_command(
            report,
            focused,
        )

        if focused.returncode != 0:
            raise RuntimeError(
                "ChatOps authenticated write focused tests failed"
            )

        security_paths = require_tests(
            root,
            [
                (
                    "services/agent_runtime/tests/"
                    "test_security_authentication.py"
                ),
                (
                    "services/agent_runtime/tests/"
                    "test_security_authentication_service.py"
                ),
                (
                    "services/agent_runtime/tests/"
                    "test_security_policy.py"
                ),
                (
                    "services/agent_runtime/tests/"
                    "test_runtime_authentication_wiring.py"
                ),
                (
                    "services/agent_runtime/tests/"
                    "test_runtime_security_policy_wiring.py"
                ),
            ],
        )

        security = run_command(
            root=root,
            name="Authentication / Security Policy compatibility",
            command=[
                "uv",
                "run",
                "pytest",
                *security_paths,
                "-q",
            ],
        )

        add_command(
            report,
            security,
        )

        if security.returncode != 0:
            raise RuntimeError(
                "Authentication / Security Policy compatibility failed"
            )

        rbac_paths = require_tests(
            root,
            [
                (
                    "services/agent_runtime/tests/"
                    "test_api_security_matrix.py"
                ),
                (
                    "services/agent_runtime/tests/"
                    "test_api_approval_rbac.py"
                ),
                (
                    "services/agent_runtime/tests/"
                    "test_api_resume_rbac.py"
                ),
                (
                    "services/agent_runtime/tests/"
                    "test_api_action_verification.py"
                ),
            ],
        )

        rbac = run_command(
            root=root,
            name="Existing API RBAC / write workflow compatibility",
            command=[
                "uv",
                "run",
                "pytest",
                *rbac_paths,
                "-q",
            ],
        )

        add_command(
            report,
            rbac,
        )

        if rbac.returncode != 0:
            raise RuntimeError(
                "Existing API RBAC / write workflow compatibility failed"
            )

        architecture = run_command(
            root=root,
            name="ChatOps authenticated-write architecture preflight",
            command=[
                "uv",
                "run",
                "python",
                "-c",
                (
                    "from pathlib import Path; "
                    "i=Path(r'services/agent_runtime/app/conversation/identity.py').read_text(encoding='utf-8'); "
                    "w=Path(r'services/agent_runtime/app/conversation/write_bridge.py').read_text(encoding='utf-8'); "
                    "o=Path(r'services/agent_runtime/app/conversation/orchestrator.py').read_text(encoding='utf-8'); "
                    "r=Path(r'services/agent_runtime/app/runtime/runtime.py').read_text(encoding='utf-8'); "
                    "print('existing_authentication='+str('AuthenticationService' in i)); "
                    "print('existing_policy='+str('SecurityPolicyEngine' in i)); "
                    "print('decide_approval='+str('ProtectedOperation.DECIDE_APPROVAL' in w)); "
                    "print('resume_action='+str('ProtectedOperation.RESUME_ACTION' in w)); "
                    "print('remediate_maps_to_resume='+str('ConversationIntent.REMEDIATE: \"action.resume\"' in o)); "
                    "print('explicit_runtime_factory='+str('create_chatops_authenticated_write_bridge' in r)); "
                    "print('automatic_write_bridge='+str('self.chatops_write_bridge =' in r)); "
                    "assert 'AuthenticationService' in i; "
                    "assert 'SecurityPolicyEngine' in i; "
                    "assert 'ProtectedOperation.DECIDE_APPROVAL' in w; "
                    "assert 'ProtectedOperation.RESUME_ACTION' in w; "
                    "assert 'ConversationIntent.REMEDIATE: \"action.resume\"' in o; "
                    "assert 'create_chatops_authenticated_write_bridge' in r; "
                    "assert 'self.chatops_write_bridge =' not in r"
                ),
            ],
        )

        add_command(
            report,
            architecture,
        )

        if architecture.returncode != 0:
            raise RuntimeError(
                "ChatOps authenticated-write architecture preflight failed"
            )

        authority = run_command(
            root=root,
            name="ChatOps identity / write authority boundary",
            command=[
                "uv",
                "run",
                "python",
                "-c",
                (
                    "from pathlib import Path; "
                    "i=Path(r'services/agent_runtime/app/conversation/identity.py').read_text(encoding='utf-8'); "
                    "w=Path(r'services/agent_runtime/app/conversation/write_bridge.py').read_text(encoding='utf-8'); "
                    "bad_identity=[x for x in ['DEFAULT_ROLE_PERMISSIONS','DEFAULT_OPERATION_PERMISSIONS','ApiKeyRecord','KubernetesProductionExecutor','httpx','requests.','aiohttp'] if x in i]; "
                    "bad_write=[x for x in ['DEFAULT_ROLE_PERMISSIONS','DEFAULT_OPERATION_PERMISSIONS','ApiKeyRecord','KubernetesProductionExecutor','httpx','requests.','aiohttp','external_actor_id'] if x in w]; "
                    "print('identity_forbidden_matches='+str(bad_identity)); "
                    "print('write_forbidden_matches='+str(bad_write)); "
                    "raise SystemExit(1 if bad_identity or bad_write else 0)"
                ),
            ],
        )

        add_command(
            report,
            authority,
        )

        if authority.returncode != 0:
            raise RuntimeError(
                "ChatOps identity / write authority boundary failed"
            )

        status = run_command(
            root=root,
            name="Git status",
            command=[
                "git",
                "status",
                "--short",
                "--",
                *[
                    str(
                        path.relative_to(
                            root
                        )
                    )
                    for path in targets
                ],
            ],
        )

        add_command(
            report,
            status,
        )

        section(
            report,
            "RESULT",
        )

        report.extend(
            [
                "PASSED",
                "",
                "ChatOps Identity + Authenticated Write Bridge v1 is installed.",
                "",
                "Human-in-the-loop flow now supported by the channel-neutral core:",
                "1. verified channel actor",
                "2. existing AuthenticationService -> OperatorIdentity",
                "3. existing SecurityPolicyEngine -> exact ProtectedOperation",
                "4. Approver may approve/reject one unique pending Approval",
                "5. Executor may resume one unique approved Action",
                "6. existing ActionRuntime durable execution boundary",
                "7. existing VerificationCoordinator after successful Action",
                "",
                "Separation of duties is preserved. Raw external_actor_id never becomes an operator_id.",
                "",
                "The write bridge remains explicitly constructed and no concrete Feishu/DingTalk/Slack transport verifier is installed yet.",
                "",
                "Next recommended step:",
                "- First Concrete ChatOps Adapter v1: choose one channel (Feishu/DingTalk/Slack), verify webhook signatures/actor identity, translate interactive buttons/messages to ChatOpsInboundMessage, and render ChatOpsWriteOutcome without changing the Runtime write boundary.",
            ]
        )

        write_text(
            after,
            "\n".join(
                report
            )
            + "\n",
        )

        print("=" * 72)
        print(
            "CHATOPS IDENTITY + AUTHENTICATED WRITE BRIDGE V1 PASSED"
        )
        print("=" * 72)
        print()
        print(
            "Installer sent no real channel/Kubernetes/Prometheus/LLM request."
        )
        print()
        print("Upload only:")
        print(after)

        return 0

    except Exception as exc:
        rollback = []

        for original, backup in reversed(
            backups
        ):
            try:
                shutil.copy2(
                    backup,
                    original,
                )

                rollback.append(
                    "RESTORED "
                    + str(
                        original.relative_to(
                            root
                        )
                    )
                )

            except Exception as rollback_exc:
                rollback.append(
                    "ROLLBACK FAILED "
                    + str(
                        original.relative_to(
                            root
                        )
                    )
                    + ": "
                    + (
                        f"{type(rollback_exc).__name__}: "
                        f"{rollback_exc}"
                    )
                )

        for path in targets:
            if (
                not preexisting[
                    path
                ]
                and path.exists()
            ):
                try:
                    path.unlink()

                    rollback.append(
                        "REMOVED newly-created "
                        + str(
                            path.relative_to(
                                root
                            )
                        )
                    )

                except Exception as rollback_exc:
                    rollback.append(
                        "ROLLBACK REMOVE FAILED "
                        + str(
                            path.relative_to(
                                root
                            )
                        )
                        + ": "
                        + (
                            f"{type(rollback_exc).__name__}: "
                            f"{rollback_exc}"
                        )
                    )

        write_text(
            error,
            "\n".join(
                [
                    (
                        "ChatOps Identity + Authenticated Write Bridge v1 FAILED"
                    ),
                    (
                        "GeneratedAt: "
                        + datetime.now().astimezone().isoformat()
                    ),
                    "",
                    f"{type(exc).__name__}: {exc}",
                    "",
                    traceback.format_exc(),
                    "",
                    "ROLLBACK",
                    "=" * 120,
                    *rollback,
                    "",
                    "PARTIAL REPORT",
                    "=" * 120,
                    *report,
                ]
            )
            + "\n",
        )

        print("=" * 72)
        print(
            "CHATOPS IDENTITY + AUTHENTICATED WRITE BRIDGE V1 FAILED"
        )
        print("=" * 72)
        print()
        print(
            "Modified files were rolled back where possible."
        )
        print()
        print("Upload only:")
        print(error)

        return 1


if __name__ == "__main__":
    raise SystemExit(
        main()
    )
